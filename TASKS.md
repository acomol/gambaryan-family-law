# TASKS — живой чек-лист

Правило: отмечать сделанное, дописывать найденное. Файл переживает сжатие контекста
длинной сессии, история диалога — нет (`~/.claude/rules/opus-5-5-working-rules.md` §4).
Подробности — в документах по ссылкам; здесь только состояние.

Источник состояния: живые readback'и координатора 2026-09-24 ~08:45 IDT (Cloudflare API, D1,
Gmail, sheet, wrangler) + репозиторий. **Обновление 24.09 вечер:** кампания
`VER1_gads_search_leads_il_family_ru_22_09_26` включена владельцем в 13:45 IDT (GAQL
`change_event`); правки формы на проде — деплой `c03e51c6` (см. «Сделано 24.09 вечер» ниже).

## Сейчас работает (проверено 2026-09-24)

- **Приём заявок**: форма lp.gambarian.com → `/api/lead` → KV + R2 + D1 (биндинги Production
  и Preview активны) → Albato webhook → сценарий GAMB_ADV → «Входящие» + Gmail. Контрольный
  лид `b4f5e872` (08:30 IDT) → письмо всем 5 получателям за 2 с.
- **Письмо о заявке (v2)**: сервер рендерит `email_html`/`email_subject`
  (`shared/lead-email.js`), деплой `1e1523a0`. Макет: компактная шапка → блок контактов →
  2 кнопки на всю ширину (Позвонить / WhatsApp) → служебный блок → «Открыть таблицу заявок»;
  плашка «Тестовая заявка» при `utm_source*test`. Приёмка владельцем на телефоне (Gmail,
  светлая/тёмная тема) — ❓ не подтверждена.
- **CRM (Google Apps Script)**: установлен владельцем 2026-09-24, standalone на
  alex@adfix.co.il. Живая проверка: G-0001…G-0006 созданы из «Входящие», статусы пишутся в
  «Журнал», «Сегодня» показывает НОВЫЕ. 8 багов, найденных только на настоящем Google, —
  исправлены; регрессия `apps-script/test/realrun.test.mjs` 207/207.
- **Бэкапы**: worker `gambarian-lead-cron` — `*/5 * * * *` (sweep, backoff 5/10/20/40 мин,
  cap 1ч) и `0 * * * *` (D1→R2 ежечасно; 02:00 UTC доп. verify/purge/heartbeat; 18:00 UTC
  reconcile + janitor удалённых заявок). `/health` (контракт pipeline-health v1):
  `backup.last_ok_at` 2026-09-24T05:00:13Z, `integrity_ok: true`, `stuck_leads: 0`. D1 Time
  Travel — восстановление на любую минуту за 7 дней, без доп. настройки, бесплатно.
- **Правило (не отменяется):** сценарий Albato GAMB_ADV **никогда** не ставить на паузу —
  пауза отвечает 200, но не обрабатывает; заявка остаётся только в D1/KV/R2 (наблюдалось
  дважды 23.09).
- Аккаунт на Workers Free (решение владельца): 5 крон-триггеров — все заняты (3 Assuta +
  2 gambarian-lead-cron), KV 1000 записей/день на весь аккаунт, D1 Time Travel не требует
  апгрейда.

## Нужно от владельца

Единый список гейтов перед включением рекламы (тот же, что в
`docs/LAUNCH-LP-GAMBARIAN.md`):

**Решения владельца по запуску (реклама уже включена):**

- Принять письмо v2 на телефоне (Gmail, светлая и тёмная тема).
- Решить: закрывать ли доступ «все по ссылке» к таблице заявок перед рекламой; сделать
  nat.shurygin@gmail.com редактором таблицы.
- [x] ~~Одобрить перенос staging → production~~ — выкачено 24.09 (`60a1cea` → Pages `c03e51c6`).
- Сказать «да» на правку конверсий Ads (решение архитектора 24.09, карта трекинга M3/M4).
  Основными остаются «Заявка — форма» `7788197742` и «Calls from ads» `7725063533` (звонок
  из объявления ≥ 60 с — план §5, решение 6). Во вспомогательные: WhatsApp `7788197745`
  (решение 5: клик ≠ сообщение), клик по телефону `7788393534`, пустая «Submit lead form»
  `7788414889` (не удалять). Ставки `MANUAL_CPC` — на расход не влияет, чинит столбец
  «Конверсии». Скрипт `ads_primary.py` исправлен (понижает 3, не 4); режим «Авто» без
  прямого подтверждения менять Ads не даёт.

**Не блокирует рекламу (housekeeping):**

- Подтвердить итоговый список эскалации (сейчас статус «pending confirmation»).
- Войти в `/api/admin` и удалить тестовую строку «ТЕСТ Активация хранилища» — вход и удаление
  владельцем пока не подтверждены.
- Секрет Google Measurement Protocol `GAMB` — удалить (значение засветилось в чате).
- Незакоммиченные правки в `gambaryan-deck-a` — удалить или оставить.
- Remote-ветки смерженных PR #3, #11 — удалить?
- Решения по `docs/TRACKING-REQUIREMENTS.md` §1: 1б (маскирование Clarity), 2а (номер 058 или
  054), пункты 3–8.

**Трекинг-гейт (отдельно от списка выше):** карта `tracking/TRACKING-MAP.md` готова (PR #16);
после деплоя `c03e51c6` — GTM v3 без изменений, билд и прод PASS. Открыто: M3/M4 (конверсии
Ads, пункт выше) и ручной «Этап 3» с телефона владельца.

Кампания включена владельцем 24.09 в 13:45 IDT — пункты письма v2 и доступа к таблице
остаются за владельцем.

## P0 — блокеры рекламы, открытые пункты

Решения по запуску — единый список в «Нужно от владельца» выше (письмо v2, доступ к таблице,
конверсии Ads). Ниже — открытые технические пункты:

- [ ] `/api/admin`: подтвердить, что владелец зашёл и удалил «ТЕСТ Активация хранилища».
- [ ] Апостроф `'+972…` перед телефоном в письме; ответ Albato 200 ≠ «письмо доставлено»;
  ошибки Albato сейчас видит только alex@adfix.co.il (раз в час).

### P0-ADS (запрос сессии Google Ads 2026-09-23) — готово, проверено live 23.09

См. `docs/ADS-HANDOFF-2026-09-23.md` (коммит `386eb5a`):

- [x] WhatsApp → номер стажёра `wa.me/972587803188`; `tel:` остаётся `972545490623`
  (решение Alex) — 5/5 на живом HTML для каждого номера.
- [x] Якоря услуг `#svc-*` — 8 штук на деплое `e6138308` (HTML `sha256 b30026ff27b0d6bc`).
- [x] Контрольная заявка «ТЕСТ»: цепочка 202 → строка во «Входящих» → `generate_lead` в
  GA4 → запрос Ads «Заявка» с `oid = submission_id` — подтверждена.

Трекинг «Этап 3» (ниже) остаётся открытым отдельно от P0-ADS — до результата карты трекинга.

### Трекинг (`docs/TRACKING-REQUIREMENTS.md`)

- [ ] Аудитории для анализа (только поток `lp`), фильтр внутреннего трафика → Active.
- [ ] Независимое ревью GTM workspace (Codex) до следующей публикации.
- [ ] Этап 3: контрольная заявка, звонок, WhatsApp с телефона владельца → DebugView, Ads.
  Гейт — карта трекинга `tracking/TRACKING-MAP.md` (в работе 24.09) — ❓ до результата, не
  отмечать сделанным.

### Репозиторий

- [x] PR #15 (тестовая строка ACTION-BAR-SPEC v2.5.0→v2.5.1) — смержен 24.09.
- [ ] SEO-слой на проде (readback 24.09) → теперь можно: карточки WhatsApp/Telegram, Rich
  Results Test (см. `docs/LAUNCH-LP-GAMBARIAN.md`).

## P1

Хвосты параметров, ранний `design_version` (сейчас `page_view` уходит без него) →
передеплой; аудитории по потоку `lp`; фильтр внутреннего трафика; Codex-ревью по итогу;
дописать документы.

## P2

Карточки WhatsApp/Telegram, Rich Results, чистка комментариев в `<head>`.

## Не удалось разрешить (для координатора)

Найдено при сверке с `docs/STATE-2026-09-24.md`, само не решается без дополнительного readback:

Сейчас пусто.

Снято: статус SEO-слоя — readback живого HTML 24.09 (деплой `c03e51c6`): noindex, canonical,
hreflang, og:image 1254×1254, JSON-LD geo на месте. Снято (решено координатором): пункт про тестовые строки во «Входящих» vs порядок
установки CRM — владелец удалил строки 24.09 ~01:30 IDT, см. «Сделано 23–24.09» ниже.

## Сделано 24.09 вечер (id для проверки)

- PR #15 и #16 смержены в `codex/final-dev5` (`564a2e2`); UX-ветка `8696946` смержена →
  `60a1cea`, `full-checks.sh` PASS, пуш в `codex/final-dev5`, деплой Pages `c03e51c6`.
- Live-readback `lp.gambarian.com`: HTML `sha256 ce53f4d00de81039`; «Отправить ещё одну
  заявку» — 0, `form-success__close` — 1, «Продолжить на сайте» — 1; `/api/admin` → 401.
- Трекинг после деплоя: `verify-tracking-map.mjs --base` (билд) PASS,
  `--live --skip-site` (прод, 10 хитов перехвачено, `G-P4MQ85ME2D`, ошибок страницы нет) PASS;
  карта обновлена под новую панель успеха (строки 42/42a).
- Ads (GAQL, read-only): кампания включена владельцем 13:45 IDT, 4 группы, объявления
  APPROVED, ключи ELIGIBLE, кроме 8 со статусом «мало запросов» (RARELY_SERVED), бюджет
  60 ILS/день, `MANUAL_CPC`;
  все 5 конверсий по-прежнему основные.

## Сделано 23–24.09 (компактно, id для проверки)

- GTM: workspace 3 → live v2 (+18 тегов, 15 триггеров, 19 переменных, Clarity → consent
  `analytics_storage`, конфликтов 0); версия 3 опубликована 23.09, `gtm.js?id=GTM-MFLHW63Q` =
  v3 внутри `G-P4MQ85ME2D` + 3 метки Ads.
- Сайт: сборка `a5d4b04` → Cloudflare деплой `a5791b9a` (main), HTML lp = сборка байт в байт
  (`sha256 602d06310dc3b4c3`), consent до GTM, `/api/lead` GET → 405.
- Живая проверка тегов на lp: `gtm.js` 1 раз, хиты только в `G-P4MQ85ME2D`,
  `traffic_type=internal`, Clarity пишет, GA4 Realtime видит поток `15824242008`; Playwright
  прогон (обычный UA) подтвердил `page_view`, `section_view`×7, `scroll_depth`×4,
  `service_select`, `contact_click` (whatsapp/phone) → Ads-клики.
- Lead pipeline перенесён с эталона Assuta → `claude/gambarian-lead-pipeline` (`6034203`,
  87/87 + 41/41); биндинги Pages Production+Preview активированы API (`943320b` →
  `codex/final-dev5`, деплой `f76780ee`); `/api/admin` 401 Basic; контрольный лид `9e758bea` →
  202 → D1 delivered + KV + R2 + строка во «Входящих».
- cron-worker `gambarian-lead-cron` задеплоен; лимит Workers Free = 5 крон/аккаунт →
  целевые `*/5` (sweep) + `0 *` (бэкап ежечасно).
- Секрет `ALBATO_WEBHOOK_URL` установлен владельцем в Worker 23.09.
- Письмо о заявке через Albato (Gmail, «4774022@gmail.com» → «4774020@gmail.com», plain
  text) — 23.09; контроль `05c8a599` 20:20:44Z → письмо 20:20:48Z всем 5.
- Round 6–7: ежечасный бэкап (первый 18:01Z `integrity_ok`), `/health`, лимит KV —
  `d20b9db`, деплой `aba60e3d` (закрывает риск гонки удаление↔запись из «раунда 5»).
- HTML-письмо v1: сервер рендерит `email_html`/`email_subject` (`shared/lead-email.js`,
  деплой `71fe20bc`); контроль `044c298b` → письмо за 2 с, `tel:+972501234567`,
  `wa.me/972501234567`, без апострофа.
- Письмо v2 по замечаниям владельца (2026-09-24 08:30 IDT) — компактная шапка, 2 кнопки
  52–56 px, служебный блок, тест-плашка; контраст ≥4.5:1; деплой `1e1523a0`, контроль
  `b4f5e872` → письмо за 2 с.
- Ресурсы CF созданы 23.09 (аккаунт `4799e9f7…`): prod KV `10bb8fd2315248139fe95118affc1515`
  / D1 `da00d8e3-477c-4863-a677-dcad24868893` / R2 `gambarian-leads-archive`; preview KV
  `552a56110ca742aa879ca06c8f4dffe4` / D1 `b57f842a-b498-4071-83a7-cee7d9aeb80b` / R2
  `gambarian-leads-archive-preview`.
- D1 миграции применены на prod и preview 23.09: `attempt_count`, `next_attempt_at`,
  таблица `cron_health`.
- Mini-CRM установлен владельцем 2026-09-24 (`claude/gambarian-mini-crm-fix` @ `bb9e218`,
  `apps-script/dist/Code.gs`); 8 найденных вживую багов исправлены (защита warning-only,
  автоконвертация «09:00» в время, ведущий 0 телефона, «нельзя удалить себя из редакторов»,
  `#REF!` в «Сегодня», watermark пропускал заявку после удаления строк, жёлтые пустые строки).
- Тестовые заявки G-0001…G-0006 → «Дубль / спам»; «Сегодня»: 0 новых.
- Тестовые строки во «Входящих» (`dd9da8b8`, `5c16e8aa`, `dbdf0083`, `9e758bea` и
  строки-призраки) — удалены владельцем 24.09 около 01:30 IDT (readback листа координатором
  показал только заголовок). Контрольный лид `b4f5e872` (08:30 IDT) стал `G-0006` «ТЕСТ
  ADFIX письмо v2 (…)» и переведён в «Дубль / спам»; readback ~08:35 IDT: «Сегодня: 0
  просрочки · 0 новых · 0 консультация(й)».
- `sheetSafe` влит в пайплайн (только на выходе в Albato, `ed4033f`).
- Получатели письма о заявке подтверждены владельцем 23.09: cityr.ta@, justicetelaviv@,
  gambarian@gmail.com, alex@adfix.co.il, nat.shurygin@gmail.com (дежурный по умолчанию).
- Draft PR #14 (`codex/final-dev5` → `main`, «final-dev5: hero, footer nav, address,
  lp.gambarian.com launch, analytics plan») — открыт, статус draft; PR #12, #13 закрыты;
  PR #3, #11 — merged.
