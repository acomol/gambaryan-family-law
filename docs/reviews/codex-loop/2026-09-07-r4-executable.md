SHA: e946b76
ЛИНЗА: executable · РАУНД: 4

НАХОДКИ (по убыванию тяжести; severity ∈ BLOCKER | DEFECT | NIT):

- [DEFECT] docs/tasks/codex/2026-09-06-final-dev4-stage-1.md:31 — «Пересборка/деплой final-dev3 и production» — пересборка запрещена, но промпт требует выполнить шаги с `build-hero-variants.py` без аргументов. Исполнитель либо нарушит запрет, либо остановится перед обязательной сборкой. Уточнение в карточке этапа 3 не входит в документы промпта этапа 1. Команды: `rg -n -o 'Пересборка/деплой final-dev3 и production|python -B scripts/build-hero-variants.py' docs/tasks/codex/2026-09-06-final-dev4-stage-1.md` → запрет на строке 31, сборка на 65/159/186; `rg -n 'rmtree|copytree|keys =|"dev3":' scripts/build-hero-variants.py` → `295: "dev3"`, `303: shutil.rmtree(dest)`, `305: shutil.copytree(SITE, dest)`, `469: keys = … or list(VARIANTS)`.

- [DEFECT] docs/tasks/codex/2026-09-06-final-dev4-stage-7.md:59 — «13 помеченных заголовков: .hero__title,.section-title,.svc-title…» — при `EXTEND_UNMARKED=no` промпт оставляет «Брачный договор» на Playfair, но обязательный `measure-fonts --expect-heading` охватывает его через общий `.svc-title`. Исполнитель должен либо изменить непомеченный заголовок вопреки параметру, либо получить FAIL предусмотренной проверки. Команды: `rg -n -o '13 помеченных заголовков: [^;]+|svc-title\[data-copy-id="3.37"\]' docs/tasks/codex/2026-09-06-final-dev4-stage-7.md` → строка 59 включает `.svc-title`, строки 131/278 выделяют `3.37` как исключение; `rg -n -F '3.37' site/index.html` → `356: <h3 class="svc-title" data-copy-id="3.37">Брачный договор</h3>`.

- [DEFECT] docs/tasks/codex/2026-09-06-final-dev4-stage-7.md:275 — «--section-pad: clamp(48px, 8vw, 80px)» — тело использует параметры владельца, промпт фиксирует числа; гейты промпта снова используют параметры. При значениях, отличающихся от умолчаний, две инструкции дают разный CSS. Команда: `rg -n -o 'clamp\(<GAP_MOBILE>px, 8vw, <GAP_DESKTOP>px\)|--precedent-reserve: <PRECEDENT_RESERVE>px|--section-pad: clamp\(48px, 8vw, 80px\)|--precedent-reserve: 80px|--expect-desktop <GAP_DESKTOP>|--expect-mobile <GAP_MOBILE>' docs/tasks/codex/2026-09-06-final-dev4-stage-7.md` → `115: clamp(<GAP_MOBILE>px, 8vw, <GAP_DESKTOP>px)` и параметр запаса; `275: clamp(48px, 8vw, 80px)` и запас `80px`; `282: --expect-desktop <GAP_DESKTOP>`, `--expect-mobile <GAP_MOBILE>`.

- [DEFECT] docs/tasks/codex/2026-09-06-final-dev4-stage-7.md:131 — «Если владельца это не устраивает, вариант „no“ не выполняется» — после инструкции исправить подстановку legacy-токена оставлено действующее условие остановки из прежнего решения. Промпт предписывает реализовать `no` с подстановкой; тело дополнительно требует вернуть вопрос владельцу из-за проблемы, которую эта подстановка устраняет. Команда: `rg -n -o 'Если владельца это не устраивает[^.]+\.|вторая подстановка[^.]+|варианты шрифтов v1–v4 перестают затрагивать заголовки|добавить его подстановку и проверку[^;]+' docs/tasks/codex/2026-09-06-final-dev4-stage-7.md` → строка 131 одновременно содержит вторую подстановку, условие отказа и утверждение «варианты… перестают затрагивать заголовки»; строка 278 требует добавить подстановку и проверку.

ПРОВЕРЕНО И ЧИСТО: Прочитаны все восемь карточек. `rg -n -o 'карточку[^;→]*stage-[1-8]\.md|stage-[1-8]\.md[^;]*целиком' docs/tasks/codex` подтвердил требование читать собственную карточку во всех восьми промптах. Проверка ссылок через Python `Path.exists()` дала `named_md_refs=32`, `missing=[]`. `git diff --check` и `git diff --stat -- docs/tasks/codex` — пустой вывод.

НЕ ПРОВЕРЕНО: Реализация этапов, сборки, браузерные и live-гейты не запускались: задание ограничено чтением, новые скрипты ещё предстоит создать. FAIL проверки шрифтов установлен как противоречие её заданных условий, а не как результат запуска будущего скрипта.