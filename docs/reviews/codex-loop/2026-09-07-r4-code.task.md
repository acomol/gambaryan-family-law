ЗАДАНИЕ РАУНДА

Ветка: fix/final-dev4-cards-codex-findings
SHA: e946b76
Рабочее дерево: DIRTY (uncommitted changes are part of the audited object)
Раунд: 4
Объект: docs/tasks/codex

Ниже бриф линзы. Следуй ему. Первой строкой ответа выведи фактический SHA, полученный командой.

# Линза `code` — существует ли то, на что ссылается документ

**Версия:** `CODEX-LENS-CODE v1.0.0` · **Дата:** `2026-09-07`

Бриф подаётся в Codex запускателем `scripts/codex-loop.ps1`. Протокол — `../CODEX-VERIFICATION-LOOP.md`.

## Роль

Ты — исполнитель замера в репозитории gambaryan-family-law, режим только для чтения. Ты не автор
и не соавтор этих документов: твоя работа — найти в них ошибку, а не одобрить их. Каждая находка
сопровождается командой и её выводом; вывод «по памяти» находкой не считается. Если сломать не
удалось — так и напиши, это тоже результат.

## Объект

Карточки `docs/tasks/codex/2026-09-06-final-dev4-stage-{1..8}.md` и код: `site/`, `scripts/`,
`site-addons/`, `.github/workflows/`.

## Что искать

1. **Несуществующий символ.** Каждое имя функции, константы, класса, файла и селектора, которое
   карточка называет, проверить grep-ом. Отсутствующее — находка.
2. **Устаревший номер строки.** Карточка ссылается на `файл:строка`; проверить `sed -n '<N>p'`,
   что там действительно то, о чём речь. Сдвиг больше чем на две строки — находка.
3. **Разошедшаяся версия.** Версия контракта или маркера в карточке против фактического значения
   в коде: `client_copy_contract.py`, `qa-browser-matrix.py`, `verify-live-previews.py`,
   `review_numbered_contract.py`, `final_dev3_contract.py`, `client-preview-map.json`.
4. **Невыполнимая правка.** Карточка предписывает изменить строку, которой в файле нет, или
   расширить условие, которое в коде выглядит иначе.
5. **Ловушка расширения.** Карточка предлагает распространить существующую проверку на новый
   вариант. Прочитай тело этой проверки целиком и скажи, какие из её утверждений на новом
   варианте станут ложными. Это уже дало один блокер: точная строка `<body class="page--final-dev3">`
   против двойного класса dev4.

## Ротация угла

Раунд 2 и выше — от кода к документу: возьми `git log` и `git diff` последних изменений кода и
найди то, что изменилось, а в карточках не отражено.

## Формат ответа

```
SHA: <вывод git rev-parse --short HEAD>
ЛИНЗА: code · РАУНД: <номер из задания>

НАХОДКИ (по убыванию тяжести; severity ∈ BLOCKER | DEFECT | NIT):
- [severity] <файл>:<строка> — <цитата ≤1 строки> — <что не так> — <команда> → <вывод>

ПРОВЕРЕНО И ЧИСТО: <что пытался сломать и не смог, командами>
НЕ ПРОВЕРЕНО: <что осталось вне охвата и почему>
```

Ничего кроме этого блока не выводи. Не предлагай новых требований и не переписывай задание.
