SHA: e946b76
ЛИНЗА: gate · РАУНД: 4

НАХОДКИ (по убыванию тяжести; severity ∈ BLOCKER | DEFECT | NIT):

- [DEFECT] docs/tasks/codex/2026-09-06-final-dev4-stage-4.md:145 — «curl -sS -A gambarian-readback … | sha256sum» — при недоступности final-dev3 проверка успешно считает хэш пустого потока; два одинаковых результата не доказывают неизменность страницы. Аналогичная команда осталась в этапе 8:148. `bash -c 'curl -sS -A gambarian-readback --max-time 3 http://127.0.0.1:1/ | sha256sum'` → stderr: `curl: (7) Failed to connect`; stdout: `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855 *-`; exit `0`. С `bash -o pipefail` → exit `7`.

- [DEFECT] docs/tasks/codex/2026-09-06-final-dev4-stage-3.md:105 — «href начинается с https://www.google.com/maps/search/?api=1&query=» — заданные проверки URL пропускают другое место, хотя шаг 2 требует сохранить конкретный адрес карты. Проверяется префикс и наличие `google.com/maps` в popup, содержимое `query` не проверяется. `python -B -c "u='https://www.google.com/maps/search/?api=1&query=Jerusalem';print(u.startswith('https://www.google.com/maps/search/?api=1&query='),'google.com/maps' in u)"` → `True True`. Это контрпример условий будущего гейта; его реализации пока нет. Негативная мутация — заменить только `query`, сохранив подпись «Карлибах, 10».

- [DEFECT] scripts/verify-live-previews.py:185 — «{len(branches)} Preview отдают текущий релиз» — пустой массив `previews` по-прежнему даёт PASS без проверки Preview при запуске без `--only`. Подмена входных данных только в памяти: `python -B -X utf8 -c "import runpy,json,sys;from unittest.mock import patch;g=runpy.run_path('scripts/verify-live-previews.py');patch('pathlib.Path.read_text',return_value=json.dumps(dict(previews=[]))).start();g['main'].__globals__['fetch']=lambda u:(200,'text/html','<meta content='+chr(34)+'noindex'+chr(34)+'>');sys.argv=['gate'];sys.exit(g['main']())"` → `PASS LIVE-PREVIEW-READBACK v1.1.0: 0 Preview отдают текущий релиз; боевой адрес не изменён`, exit `0`.

ПРОВЕРЕНО И ЧИСТО:

- `python -B -X utf8 -`: вызовы `main()` локальных валидаторов с подменой `Path.exists/is_file` для отсутствующего `build/variants/` → оба возвращают `1`; copy сообщает 14 отсутствующих HTML, previews — 7 отсутствующих сборок.
- `verify-live-previews.main()` с `--only final-dev3` и подменой `fetch` на `OSError('offline')` → FAIL final-dev3, FAIL production, exit `1`.
- `python -B -S scripts/qa-browser-matrix.py http://127.0.0.1:8098/ --all-previews` → `ModuleNotFoundError: No module named 'playwright'`, exit `1`.
- Через `python -B -X utf8 -` выполнены 10 существующих unit-тестов с заменой временных HTML-файлов объектами в памяти → `Ran 10 tests`, `OK`.
- `verify_frozen_source()` и `verify_html(site/index.html)` → `[]`; замена `&nbsp;` на U+00A0 в памяти также → `[]`.
- `git diff --check` → пусто, exit `0`.

НЕ ПРОВЕРЕНО: исчерпывающий перебор всех команд карточек, реальные сетевые readback, browser matrix и сборщики. `verify-address-links.py`, `verify-business-hours.py` и сборка final-dev4 отсутствуют. Полный файловый unittest-прогон и пересборки не выполнялись из-за режима только чтения; мутации выполнялись в памяти.