SHA: 8f301cb
ЛИНЗА: executable · РАУНД: 1

НАХОДКИ (по убыванию тяжести; severity ∈ BLOCKER | DEFECT | NIT):

- [DEFECT] docs/tasks/codex/2026-09-06-final-dev4-stage-3.md:32 — «final-dev3 и production не пересобираются» — запрет несовместим с обязательным запуском `build-hero-variants.py` без аргументов: он пересоздаёт каталог final-dev3. Исполнитель должен либо нарушить запрет, либо пропустить обязательную сборку. — `rg -n 'keys =|dest = build|shutil.rmtree|shutil.copytree' scripts/build-hero-variants.py` → `303: shutil.rmtree(dest)`, `305: shutil.copytree(SITE, dest)`, `469: keys = … or list(VARIANTS)`, `475: dest = build(k)`; `git`-источник builder-а содержит `VARIANTS["dev3"]` на строке 295.

- [DEFECT] docs/tasks/codex/2026-09-06-final-dev4-stage-3.md:136 — «python -B scripts/verify-client-copy.py» — в обязательном порядке гейт запускается перед пересборкой Preview на строке 138, хотя контракт уже запрещает старые адресные атрибуты. Старые сборки дадут FAIL; на чистом checkout сборок ещё нет. Промпт, напротив, требует сначала пересобрать всё. Аналогичное расхождение — этап 4, строки 133–135 против 220. — `rg -n 'targets.append|source:standalone|index.html не найден' scripts/verify-client-copy.py` → `341: source:standalone`, `364: targets.append((f"preview:{branch}", … / "index.html"))`, `385: index.html не найден`. Проверка действующим verifier с новым allowlist в памяти → `build\variants\final-dev3\index.html: неизвестный текстовый атрибут/meta: 'Открыть адрес в Google Maps: Тель-Авив, Карлибах 10'`.

- [DEFECT] docs/tasks/codex/2026-09-06-final-dev4-stage-4.md:40 — «путь адаптера в DYNAMIC_UI_PATHS» — проверка шага 1 требует запись, которую создаёт шаг 2. Затем шаг 2 требует PASS builder-а уже с новыми required-токенами, хотя адаптер получает соответствующее поведение только в шаге 6. Указанный порядок невыполним без переноса проверок. — `rg -n -o 'путь адаптера в DYNAMIC_UI_PATHS|DYNAMIC_UI_PATHS \+= site-addons/final-dev4/hero-business-hours.js|### [236]\..*|все варианты «проверка пройдена»' docs/tasks/codex/2026-09-06-final-dev4-stage-4.md` → `40: путь адаптера…`, `42: ### 2. Завести собственный адаптер…`, `46: DYNAMIC_UI_PATHS += …`, `48: все варианты «проверка пройдена»`, `74: ### 6. Расширить dev4-адаптер…`.

- [DEFECT] docs/tasks/codex/2026-09-06-final-dev4-stage-5.md:52 — «Три карточки .facts-grid заменить целиком на» — буквальный HTML из карточки не содержит пробелов между текстами соседних div. Действующий parser склеивает заголовок и подзаголовок; все три строки расходятся с контрактом шага 5. — `python -B -c "import runpy,sys; from pathlib import Path; sys.path.insert(0,'scripts'); m=runpy.run_path('scripts/verify-client-copy.py'); p=m['CopyHTMLParser'](); s=Path('docs/tasks/codex/2026-09-06-final-dev4-stage-5.md').read_text(encoding='utf-8').splitlines()[51]; p.feed(s[s.index('<div class='):]); print([(n.copy_id,n.text) for n in p.nodes])"` → склейки `30+ летпрофессиональный`, `практикевозвращение`, `статейВ области`; сравнение с тремя строками контракта → `FAIL` для каждого owner-id.

- [DEFECT] docs/tasks/codex/2026-09-06-final-dev4-stage-7.md:278 — «--font-serif … → семейство заголовков» — при `EXTEND_UNMARKED=no` тело требует оставить общий токен Playfair и создать отдельный `--font-heading`; промпт безусловно меняет общий токен. Дополнительно промпт удаляет italic-файлы всегда, хотя тело сохраняет их при оставшихся italic-потребителях. Получаются разные шрифты непомеченных заголовков. — `rg -n -o -- 'при EXTEND_UNMARKED=no значение --font-serif НЕ менять|--font-serif \(имя токена НЕ менять\) → семейство заголовков|italic всегда|italic-файлы уходят только[^;]+' docs/tasks/codex/2026-09-06-final-dev4-stage-7.md` → противоположные инструкции на строках `131`, `277`, `278`.

- [DEFECT] docs/tasks/codex/2026-09-06-final-dev4-stage-7.md:59 — «--expect-heading задан и хотя бы один селектор из списка отрисован не этим семейством» — при `EXTEND_UNMARKED=no` разрешено сохранить Playfair у непомеченных элементов, но они включены в стандартный список селекторов гейта. Обязательная команда на строке 133 требует для них новое семейство и ожидает exit 0. Даже исполнение тела карточки приведёт к FAIL. — `rg -n -o -- '--selectors \(по умолчанию[^)]+|--expect-heading задан[^;]+|Playfair для непомеченных|--expect-heading.*?exit 0' docs/tasks/codex/2026-09-06-final-dev4-stage-7.md` → `59: … .svc-media__name,.form-success__title плюс заголовки кубиков…`, `131: Playfair для непомеченных`, `133: --expect-heading … → exit 0`.

- [DEFECT] docs/tasks/codex/2026-09-06-final-dev4-stage-7.md:91 — «параметры --ratio 4:3|1:1» — предусмотренный сценарий пустого `PHOTO_RATIO` требует кроп 4:5, но интерфейс создаваемого скрипта разрешает только 4:3 и 1:1. Команда для обязательного сценария без решения владельца не определена. — `rg -n -o -- 'параметры --ratio 4:3\|1:1|при пустом — 1100×1375' docs/tasks/codex/2026-09-06-final-dev4-stage-7.md` → `91: параметры --ratio 4:3|1:1`, `93: при пустом — 1100×1375`.

- [DEFECT] docs/tasks/codex/2026-09-06-final-dev4-stage-7.md:107 — «В блоке @media (max-width: 860px) удалить правило» — тело безусловно удаляет мобильные `16/15` и `center 16%`; промпт разрешает удаление только после подтверждающего замера. При неподтверждённом замере первое прочтение меняет мобильный кадр, второе сохраняет его; приёмка на строке 205 требует безусловного удаления. — `rg -n -o -- 'В блоке @media \(max-width: 860px\) удалить правило|mobile-правила 16 / 15 и center 16% удалить только если замер верха головы это подтверждает' docs/tasks/codex/2026-09-06-final-dev4-stage-7.md` → безусловное указание на `107`, условное на `275`.

ПРОВЕРЕНО И ЧИСТО: прочитаны в UTF-8 все восемь карточек целиком и README; проверены относительные Markdown-ссылки: `50`, отсутствующих — `0`. Ссылки на будущие гейты этапа 8 сопоставлены с шагами их создания в этапах 3, 4 и 7. `git diff --check` → пустой вывод. `git rev-parse --short HEAD` → `8f301cb`.

НЕ ПРОВЕРЕНО: фактическое исполнение этапов, browser-матрица, GitHub/Cloudflare и live-readback: задание ограничено чтением; изменения, сборки, публикации и отправка лидов не выполнялись.