SHA: 3e8d13f
ЛИНЗА: gate · РАУНД: 3

НАХОДКИ (по убыванию тяжести; severity ∈ BLOCKER | DEFECT | NIT):

- [DEFECT] docs/tasks/codex/2026-09-06-final-dev4-stage-1.md:169 — «curl -sA … | sha256sum (до и после — совпадает)» — при недоступности final-dev3 оба замера дадут одинаковый хэш пустого потока; ошибка curl скрыта, pipeline успешен. Контрпример в Git Bash: `curl -sA gambarian-readback http://127.0.0.1:1/ | sha256sum` → `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855 *-`, exit `0`, stderr пустой. Та же команда с `bash -o pipefail` → exit `7`. Совпадение этих хэшей не доказывает неизменность final-dev3.

- [DEFECT] docs/tasks/codex/2026-09-06-final-dev4-stage-4.md:173 — «Подтверждение, что git diff site-addons/final-dev3 site-addons/action-bar пуст» — проверка пропускает уже закоммиченные изменения защищённых каталогов: сравнивает рабочие файлы с индексом, без базы этапа. `git diff --stat site-addons/final-dev3 site-addons/action-bar` → пусто; `git diff --stat 75558d9 HEAD -- site-addons/final-dev3 site-addons/action-bar` → `4 files changed, 26 insertions(+), 5 deletions(-)`. Исторический диапазон здесь — контрпример семантики команды, не утверждение о нарушении этапа 4.

- [DEFECT] scripts/verify-live-previews.py:185 — «{len(branches)} Preview отдают текущий релиз» — пустой массив `previews` даёт PASS без проверки единого Preview. Воспроизведение с подменой входных данных только в памяти: `python -B -X utf8 -c "import runpy,json,sys; from unittest.mock import patch; g=runpy.run_path('scripts/verify-live-previews.py'); patch('pathlib.Path.read_text',return_value=json.dumps(dict(previews=[]))).start(); g['main'].__globals__['fetch']=lambda url:(200,'text/html','<meta content='+chr(34)+'noindex'+chr(34)+'>'); sys.argv=['gate']; sys.exit(g['main']())"` → `PASS LIVE-PREVIEW-READBACK v1.1.0: 0 Preview отдают текущий релиз; боевой адрес не изменён`, exit `0`. Контрпример относится к запуску без `--only`.

- [DEFECT] docs/tasks/codex/2026-09-06-final-dev4-stage-7.md:117 — «grep -n -- … -A1 site/styles.css» — после `--` параметр `-A1` становится именем файла; проверка завершится ошибкой независимо от правильности CSS. `grep -n -- '--section-pad:\|--precedent-reserve:\|precedent__container\|\.precedent-card {' -A1 site/styles.css` → совпадения из CSS, затем `grep: -A1: No such file or directory`, exit `2`.

ПРОВЕРЕНО И ЧИСТО:

- Вызовы `main()` обоих локальных валидаторов через `python -B -X utf8 -` с `unittest.mock.patch` для отсутствующих файлов `build/variants/` → exit `1`; сообщения «index.html не найден» / «нет собранного …/index.html».
- `verify-live-previews.main()` с `--only final-dev3` и подменой `fetch` на `OSError('offline')` → FAIL final-dev3, FAIL production, exit `1`.
- `python -B -S scripts/qa-browser-matrix.py http://127.0.0.1:8098/ --all-previews` → `ModuleNotFoundError: No module named 'playwright'`, exit `1`.
- `unittest.main(... discover -s scripts/tests)` с пустым результатом `os.listdir` → `Ran 0 tests`, `NO TESTS RAN`, фактический exit `5`.
- `verify_html(site/index.html)` после замены `&nbsp;` на U+00A0 только в памяти → `[]`: эквивалентный пробел ложного FAIL не вызывает.

НЕ ПРОВЕРЕНО: полный перебор проверок карточек, browser matrix и реальные сетевые readback. `verify-address-links.py`, `verify-business-hours.py` и сборка final-dev4 отсутствуют; будущие реализации не проверялись. Пересборка и полный unit-прогон не выполнялись: они записывают файлы, тогда как задание допускает только чтение.