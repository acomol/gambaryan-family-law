SHA: e946b76
ЛИНЗА: code · РАУНД: 4

НАХОДКИ (по убыванию тяжести; severity ∈ BLOCKER | DEFECT | NIT):

- [DEFECT] docs/tasks/codex/2026-09-06-final-dev4-stage-7.md:59 — «--selectors (по умолчанию только 13 помеченных заголовков: … .svc-title …)» — селектор включает непомеченный «Брачный договор». Шаг 11 (:131) сохраняет ему Playfair при `EXTEND_UNMARKED=no`, поэтому гейт с `--expect-heading Onest/Golos/Manrope` отвергнет предусмотренный карточкой результат. После удаления двух H2 селекторы охватывают 14 заголовков. — `rg -n 'svc-title.*3\.37' site/index.html` → `356: <h3 class="svc-title" data-copy-id="3.37">Брачный договор</h3>`; разбор через `HTMLParser` → `current selector matches: 16; after removing facts H2 and 5.6: 14; unmarked 3.37 still matched: [(356, 'svc-title', '3.37')]`.

- [DEFECT] docs/tasks/codex/2026-09-06-final-dev4-stage-1.md:15 — «байт в байт равно текущему site/ (то есть final-dev3) плюс маркер dev4» — `site/` не равен сборке final-dev3: builder удаляет `.nav-call`, меняет Hero и добавляет CSS. Предписанное наследование через `variant_final_dev3()` не обеспечивает заявленное равенство даже после удаления маркеров. — `python -B -X utf8 -c "import runpy,sys; from pathlib import Path; sys.path.insert(0,'scripts'); b=runpy.run_path('scripts/build-hero-variants.py'); c=runpy.run_path('scripts/final_dev3_contract.py'); s=Path('site/index.html').read_text(encoding='utf-8'); h,css=b['variant_final_dev3'](s); print(c['normalize_html'](h)==s, len(c['normalize_css'](css)))"` → `False 2963`.

ПРОВЕРЕНО И ЧИСТО:

- Ротация от кода: `git log -5 --oneline -- site scripts site-addons .github/workflows` и `git diff c5e90e5..HEAD -- .github/workflows/deploy-previews.yml` — изменения диагностики Cloudflare дополнительного расхождения с карточками не выявили.
- Исходные версии через `runpy.run_path`: copy `1.2.0`, review `2.0.1`, dev3 `2.0.2`, readback `1.1.0`, runner `1.4.1`; JSON-карта `2.4.0`, 11 alias — соответствуют исходным значениям переходов.
- Проверены расширяемые условия Hero/dev3 и аккордеона через чтение `verify()` и `validate_metrics()`: замена точного body-тега предусмотрена этапом 1; отсутствие `<p>` после этапа 5 отключает требование тумблера.
- `verify_html(Path('site/index.html'))` → `[]`.
- `git diff --stat HEAD -- docs/tasks/codex site scripts site-addons .github/workflows` → пусто; незакоммиченные файлы — задания раунда.

НЕ ПРОВЕРЕНО: исчерпывающая сверка каждого символа и каждого номера строки всех восьми карточек не завершена. Полная сборка будущих этапов, browser-матрица и live-readback не выполнялись: реализация dev4 отсутствует, режим только для чтения.