SHA: 3e8d13f
ЛИНЗА: executable · РАУНД: 3

НАХОДКИ (по убыванию тяжести; severity ∈ BLOCKER | DEFECT | NIT):

- [DEFECT] docs/tasks/codex/2026-09-06-final-dev4-stage-1.md:243 — «Сделай ровно шаги 2–15 карточки, в её порядке» — Шаг 6 требует PASS `verify-client-copy`, но сборка остальных Preview назначена шагом 15. На чистом checkout исполнитель остановится: verifier читает все каталоги карты, включая ещё отсутствующие шрифтовые варианты. Команды: `rg -n -o 'Проверка: python -B scripts/verify-client-copy.py → PASS|### 15\. Пересобрать|в её порядке' docs/tasks/codex/2026-09-06-final-dev4-stage-1.md`; `rg -n 'targets.append\(\(f|index.html не найден' scripts/verify-client-copy.py` → `81: … → PASS`; `147: ### 15. Пересобрать`; `243: в её порядке`; код: `364: … ROOT / relative / "index.html"`; `385: … index.html не найден`.

- [DEFECT] docs/tasks/codex/2026-09-06-final-dev4-stage-4.md:48 — «python -B scripts/build-hero-variants.py → все варианты „проверка пройдена“» — На шаге 2 копируется прежний адаптер и вводятся required-токены `data-business-closed`, но поведение с этими селекторами появляется только на шаге 6. Проверка копии до расширения адаптера не пройдёт. Команда: `rg -n -o 'SCRIPT_REQUIRED_TOKENS[^;]*|### 6\. Расширить|Проверка: test -f[^;]*' docs/tasks/codex/2026-09-06-final-dev4-stage-4.md` → `46: …селекторы вне Hero, data-business-closed`; `48: Проверка…`; `74: ### 6. Расширить`. Чтение исходного адаптера подтвердило отсутствие обоих селекторов; `scripts/build-hero-variants.py:446` проверяет `any(token not in script for token in required_script_tokens)`.

- [DEFECT] docs/tasks/codex/2026-09-06-final-dev4-stage-7.md:278 — «при EXTEND_UNMARKED=no правилам .svc-media__name … и .form-success__title … прописать явный Playfair» — Заголовки кубиков пропущены. Они получают `var(--font-serif)` на этапе 5, поэтому сменят семейство даже при `no`, хотя шаг 2 этапа 7 относит их к исключённым. Два прочтения дают разные результаты: сохранить Playfair всем непомеченным либо только двум перечисленным элементам. Команда: `rg -n -o 'заголовки кубиков этапа 5 добавляются[^—]*|при EXTEND_UNMARKED=no правилам[^;]*' docs/tasks/codex/2026-09-06-final-dev4-stage-7.md` → `59: заголовки кубиков… ТОЛЬКО при EXTEND_UNMARKED=yes`; `278: … .svc-media__name … и .form-success__title …`.

- [DEFECT] docs/tasks/codex/2026-09-06-final-dev4-stage-7.md:125 — «иначе 2 — только normal» — Для `EXTEND_UNMARKED=no` проверка требует два файла Playfair, хотя инструкция сохраняет italic-потребителей и соответствующие файлы. Исполнитель либо получает ложный FAIL, либо удаляет необходимые italic-файлы. Команды: `rg -n -o 'иначе 2 — только normal|EXTEND_UNMARKED=no italic-потребители остаются[^)]*' docs/tasks/codex/2026-09-06-final-dev4-stage-7.md`; `rg -n 'font-style: italic' site/fonts.css` → `123: …italic-потребители остаются, и файлы остаются тоже`; `125: иначе 2 — только normal`; `70/80: font-style: italic;`. Разбор `@font-face` подтвердил: `Playfair faces: 4 italic=2 normal=2`.

- [DEFECT] docs/tasks/codex/2026-09-06-final-dev4-stage-7.md:284 — «одна строка superseded» — Промпт безусловно требует объявить прежний мобильный контракт отменённым. Тело разрешает эту запись только после фактического снятия компенсации; сам промпт допускает её сохранение. При сохранённой компенсации исполнитель задокументирует несуществующее изменение. Команда: `rg -n -o 'строку добавлять только если компенсация действительно снята|одна строка superseded' docs/tasks/codex/2026-09-06-final-dev4-stage-7.md` → `155: строку добавлять только если компенсация действительно снята`; `284: одна строка superseded`.

ПРОВЕРЕНО И ЧИСТО: UTF-8-чтение карточек 1–8 и README; проверка Python через `Path.is_file()` всех конкретных `docs/*.md`, названных в промптах, → для каждого этапа `missing=[]`; все восемь промптов требуют прочитать собственную карточку (`self_reference=True`). Ручные действия владельца по деплою названы явно. `git diff --name-only` и `git diff --cached --name-only` → пусто; незакоммиченные файлы — четыре брифа раунда 3.

НЕ ПРОВЕРЕНО: фактическая реализация будущих этапов, полные сборки, браузерные и live-гейты, GitHub/Cloudflare. Проверка проводилась только чтением; отсутствие будущих файлов само по себе дефектом не считалось.