ЗАДАНИЕ РАУНДА

Ветка: fix/final-dev4-cards-codex-findings
SHA: 8f301cb
Рабочее дерево: DIRTY (uncommitted changes are part of the audited object)
Раунд: 1
Объект: docs/tasks/codex

Ниже бриф линзы. Следуй ему. Первой строкой ответа выведи фактический SHA, полученный командой.

# Линза `executable` — сможет ли исполнитель это выполнить

**Версия:** `CODEX-LENS-EXECUTABLE v1.0.0` · **Дата:** `2026-09-07`

Бриф подаётся в Codex запускателем `scripts/codex-loop.ps1`. Протокол — `../CODEX-VERIFICATION-LOOP.md`.

## Роль

Ты — исполнитель замера в репозитории gambaryan-family-law, режим только для чтения. Ты не автор
и не соавтор этих документов: твоя работа — найти в них ошибку, а не одобрить их. Каждая находка
сопровождается командой и её выводом; вывод «по памяти» находкой не считается. Если сломать не
удалось — так и напиши, это тоже результат.

## Объект

Каждая карточка целиком, с особым вниманием к блоку «Промпт для Codex» в её конце.

## Что искать

1. **Тело против промпта.** Карточка содержит две копии указаний: тело шагов и блок промпта.
   Исполнитель вставляет промпт. Для каждого параметра, версии, условия и запрета сверь обе копии
   и покажи расхождение. Этот дефект уже случался трижды: правка вносилась в тело и не вносилась
   в промпт.
2. **Место остановки.** Найди первое место, где исполнителю не хватит данных: неизвестное
   значение параметра, ссылка на несуществующий документ, «см. решение владельца» без решения.
3. **Двусмысленность.** Указание, допускающее два прочтения с разным результатом. Приведи оба.
4. **Недостижимая проверка.** Команда в шаге, которую нельзя выполнить в описанном порядке:
   ссылается на файл, создаваемый позже, или на сборку, которой ещё нет.
5. **Скрытый ручной шаг.** Место, где карточка предполагает действие владельца, не назвав его.

## Ротация угла

Раунд 2 и выше — промпт в одиночку: считай, что исполнитель прочёл только блок «Промпт для Codex»
и перечисленные в нём документы. Что он сделает неправильно.

## Формат ответа

```
SHA: <вывод git rev-parse --short HEAD>
ЛИНЗА: executable · РАУНД: <номер из задания>

НАХОДКИ (по убыванию тяжести; severity ∈ BLOCKER | DEFECT | NIT):
- [severity] <файл>:<строка> — <цитата ≤1 строки> — <что не так> — <команда> → <вывод>

ПРОВЕРЕНО И ЧИСТО: <что пытался сломать и не смог, командами>
НЕ ПРОВЕРЕНО: <что осталось вне охвата и почему>
```

Ничего кроме этого блока не выводи. Не предлагай новых требований и не переписывай задание.
