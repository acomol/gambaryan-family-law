SHA: 3e8d13f
ЛИНЗА: code · РАУНД: 3

НАХОДКИ (по убыванию тяжести; severity ∈ BLOCKER | DEFECT | NIT):

- [DEFECT] docs/tasks/codex/2026-09-06-final-dev4-stage-7.md:131 — «прописывается явное `font-family: "Playfair Display", Georgia, "Times New Roman", serif`» — при `EXTEND_UNMARKED=no` два селектора сохраняют Playfair, но builder v2–v4 удаляет исходные шрифты и создаёт только выбранные пары. Явный Playfair регулярка не заменяет; для этих элементов теряется локальный шрифт. — `rg -n 'rmtree|for role|--font-serif:' scripts/build-font-variants.py` → `181: shutil.rmtree(dest / "fonts")`, `197: for role in ("serif", "body")`, `249: styles = re.sub(r'--font-serif:[^;]+;', …)`; `python -B -c "import sys,runpy; sys.path.insert(0,'scripts'); v=runpy.run_path('scripts/build-font-variants.py')['VARIANTS']; print({n:[c[r]['family'] for r in ('serif','body')] for n,c in v.items() if n>1})"` → `{2: ['Lora', 'Inter'], 3: ['Literata', 'Manrope'], 4: ['PT Serif', 'Golos Text']}`.

- [DEFECT] docs/tasks/codex/2026-09-06-final-dev4-stage-7.md:131 — «снизить только максимум clamp .hero__title … (58 → 54px)» — предписанная правка не уменьшает H1 на целевых 360/390px, поэтому не исправляет описанный выход CTA за первый экран. На обеих ширинах действует нижняя граница `32px`. — `rg -n -F 'font-size: clamp(32px, 4.6vw, 58px)' site/styles.css` → `430: font-size: clamp(32px, 4.6vw, 58px);`; `python -B -c "print([(w,max(32,min(4.6*w/100,58)),max(32,min(4.6*w/100,54))) for w in (360,390)])"` → `[(360, 32, 32), (390, 32, 32)]`.

- [NIT] docs/tasks/codex/2026-09-06-final-dev4-stage-7.md:139 — «строка 13 docstring (“Font coverage includes title, italic service heading” …)» — нужный текст находится на строке 30, расхождение составляет 17 строк. — `python -B -c "from pathlib import Path; t=Path('scripts/qa-browser-matrix.py').read_text(encoding='utf-8').splitlines(); print('13:',t[12]); print('30:',t[29])"` → `13: 5 breakpoint/landscape viewports…`; `30: geometry also passed. Font coverage includes title, italic service heading,`.

ПРОВЕРЕНО И ЧИСТО:

- Ротация от кода: `git log -8 --oneline -- site scripts site-addons .github/workflows` и `git diff c5e90e5..HEAD -- .github/workflows site scripts site-addons ':!scripts/codex-loop.ps1'` — изменения диагностики Cloudflare не обнаружили дополнительного расхождения с карточками.
- Прочитаны полные тела расширяемых проверок Hero/dev3, review-numbered и проверок карточек фактов runner-а. Учтённая этапом 1 замена точного body-тега устраняет указанную ловушку двойного класса; отсутствие `<p>` после этапа 5 не активирует требование тумблера.
- Значения контрактов прочитаны через `runpy.run_path`: copy `1.2.0`, review `2.0.1`, dev3 `2.0.2`, readback `1.1.0`; карта — `2.4.0`, 11 alias. Они соответствуют исходным значениям переходов карточек.
- Вызовы `verify_frozen_source()` и `verify_html(Path('site/index.html'))` → `[]`, `[]`.
- `git diff --name-only HEAD -- docs/tasks/codex site scripts site-addons .github/workflows` → пусто; SHA повторно подтверждён.

НЕ ПРОВЕРЕНО: полная сборка будущих этапов, browser-матрица и live-readback — реализация dev4 ещё отсутствует, аудит выполнялся только для чтения. Исчерпывающая сверка каждого упоминания символа и каждого номера строки не завершена; полный PASS карточкам не заявляется.