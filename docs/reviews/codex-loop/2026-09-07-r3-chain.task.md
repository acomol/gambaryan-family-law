ЗАДАНИЕ РАУНДА

Ветка: fix/final-dev4-cards-codex-findings
SHA: 3e8d13f
Рабочее дерево: DIRTY (uncommitted changes are part of the audited object)
Раунд: 3
Объект: docs/tasks/codex

Ниже бриф линзы. Следуй ему. Первой строкой ответа выведи фактический SHA, полученный командой.

# Линза `chain` — сходится ли между этапами

**Версия:** `CODEX-LENS-CHAIN v1.0.0` · **Дата:** `2026-09-07`

Бриф подаётся в Codex запускателем `scripts/codex-loop.ps1`. Протокол — `../CODEX-VERIFICATION-LOOP.md`.

## Роль

Ты — исполнитель замера в репозитории gambaryan-family-law, режим только для чтения. Ты не автор
и не соавтор этих документов: твоя работа — найти в них ошибку, а не одобрить их. Каждая находка
сопровождается командой и её выводом; вывод «по памяти» находкой не считается. Если сломать не
удалось — так и напиши, это тоже результат.

## Объект

Восемь карточек как одна последовательность плюс `docs/tasks/codex/README.md`.

## Что искать

1. **Версии.** Цепочка каждого контракта по этапам: значение после этапа N должно совпадать с
   предпосылкой этапа N+1. Контракты: CLIENT-COPY-CONTRACT, CLIENT-COPY-VERIFIER,
   PREVIEW-BROWSER-QA-RUNNER, LIVE-PREVIEW-READBACK, REVIEW-NUMBERED, FINAL-DEV4-DESIGN,
   ACTION-BAR-SPEC, карта Preview, BUILD-TOOLS.
2. **Счётчики.** Число unit-тестов, число защищённых тире `&nbsp;—`, число owner-id, число alias
   Preview — по этапам без разрывов.
3. **Предпосылки.** Каждое утверждение вида «после этапа N в дереве есть X» сверить с тем, что
   этап N действительно создаёт. Особый случай: этап требует как готовое то, что создаёт сам.
4. **Кто создаёт артефакт.** Для каждого файла, который карточки упоминают как существующий,
   найти этап-создатель. Файл без создателя — находка. Артефакт, который создают дважды, — тоже.
5. **Порядок.** Зависимости скриптов и данных: гейт, который вводит этап N, используется этапами
   после него, а не до.

## Ротация угла

Раунд 2 и выше — слияние не по порядку: что сломается, если этап 5 вольют раньше этапа 4, и
названо ли это в карточках.

## Формат ответа

```
SHA: <вывод git rev-parse --short HEAD>
ЛИНЗА: chain · РАУНД: <номер из задания>

НАХОДКИ (по убыванию тяжести; severity ∈ BLOCKER | DEFECT | NIT):
- [severity] <файл>:<строка> — <цитата ≤1 строки> — <что не так> — <команда> → <вывод>

ПРОВЕРЕНО И ЧИСТО: <что пытался сломать и не смог, командами>
НЕ ПРОВЕРЕНО: <что осталось вне охвата и почему>
```

Ничего кроме этого блока не выводи. Не предлагай новых требований и не переписывай задание.
