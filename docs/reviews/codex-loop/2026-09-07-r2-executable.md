SHA: ca70b9e
ЛИНЗА: executable · РАУНД: 2

НАХОДКИ (по убыванию тяжести; severity ∈ BLOCKER | DEFECT | NIT):

- [DEFECT] docs/tasks/codex/2026-09-06-final-dev4-stage-7.md:279 — «EXPECTED_FONTS (…) для всех не-v alias» — при `EXTEND_UNMARKED=no` промпт переводит заголовки на `--font-heading`, который builder вариантов не подменяет. Шаг 12 сохраняет прежние ожидания v1–v4 и требует общий PASS. Исполнитель получает несовместимые требования: сохранить предписанную реализацию или изменить запрещённые ожидания/варианты. — `rg -n -- 'font-heading|--font-serif:|expected_fonts\[0\]' scripts/build-font-variants.py scripts/qa-browser-matrix.py` → builder:249–250 подменяет только `--font-serif`; runner:990–991 проверяет семейство заголовков по `expected_fonts[0]`. Проверка подстановки в памяти для разрешённого `FONT_HEADINGS=onest, EXTEND_UNMARKED=no` → `actual=Onest`, ожидания `Playfair Display / Lora / Literata / PT Serif`, все четыре `match=False`.

- [DEFECT] docs/tasks/codex/2026-09-06-final-dev4-stage-7.md:101 — «`ls site/assets | grep -c 'alexander-card-v2\|^yulia-card-' → 0`» — новые обязательные файлы `yulia-card-v3-*` тоже соответствуют этому шаблону: проверка потребует ноль при шести новых файлах. Соседнее указание удалить «старые … `yulia-card-*`» допускает удаление новых ассетов при буквальном применении маски. — `python -B -c "import fnmatch,re; print(fnmatch.fnmatch('yulia-card-v3-1100w.12345678.jpg','yulia-card-*')); print(bool(re.search(r'alexander-card-v2|^yulia-card-', 'yulia-card-v3-1100w.12345678.jpg')))"` → `True`, `True`.

- [DEFECT] docs/tasks/codex/2026-09-06-final-dev4-stage-4.md:133 — «`python -B scripts/verify-client-copy.py`» — раздел «Гейты (в этом порядке)» запускает verifier до сборки Preview на строке 135; промпт:220 задаёт обратный порядок. На чистом checkout выполнение тела останавливается на отсутствующих сборках. — `python -B -c "import runpy,sys; from pathlib import Path; sys.path.insert(0,'scripts'); m=runpy.run_path('scripts/verify-client-copy.py'); print(m['verify_html'](Path('build/variants/final-dev4/index.html')))"` → `['build\\variants\\final-dev4\\index.html: index.html не найден']`. Чтение строк 132–135 → `build-preview → verify-client-copy → unittest → builders`.

- [DEFECT] docs/tasks/codex/2026-09-06-final-dev4-stage-7.md:123 — «italic — всегда» — тело требует безусловно удалить italic-файлы Playfair, промпт разрешает удаление только после исчезновения всех italic-потребителей. При `EXTEND_UNMARKED=no` два прочтения дают разные наборы физических шрифтов; `.svc-media__name` и `.form-success__title` сохраняют Playfair italic. — `rg -n -o 'italic — всегда|italic-файлы — только[^;]+' docs/tasks/codex/2026-09-06-final-dev4-stage-7.md` → `123:italic — всегда`; `277:italic-файлы — только если после правок ни одно правило не осталось italic …`. Чтение CSS обоих селекторов → `italic=True`, `serif=True`.

- [DEFECT] docs/tasks/codex/2026-09-06-final-dev4-stage-7.md:99 — «width/height img → 1100 и 825» — шаг переключения HTML учитывает 4:3 и 1:1, но пропускает разрешённый сценарий пустого `PHOTO_RATIO`. Для него генератор и приёмка требуют `1100×1375`. Буквальное выполнение шага оставляет неверный `height`, несмотря на параметрический промпт. — `rg -n -o '1100 и 825[^;]+|1100×1375[^;]+' docs/tasks/codex/2026-09-06-final-dev4-stage-7.md` → `93:1100×1375 (окно 4/5 без изменений)`; `99:1100 и 825 …`; `204:1100×1375 (пустой PHOTO_RATIO)`.

- [DEFECT] docs/tasks/codex/2026-09-06-final-dev4-stage-4.md:38 — «если нет, это дефект этапа 1» — исполнитель должен объявить отсутствие собственной поддержки класса dev4 в Action Bar дефектом, хотя шаг 2 намеренно сохраняет наследуемый класс dev3. Прочтение проверки как требования отдельного класса даёт ложный дефект; проверка фактического наследуемого поведения его не даёт. — `rg -n 'finalDev3TopOnly =' site-addons/action-bar/action-bar.js` → `50: …classList.contains('page--final-dev3')`; `rg -n -o 'если нет, это дефект этапа 1|класс page--final-dev3 в body dev4 сохраняется намеренно' docs/tasks/codex/2026-09-06-final-dev4-stage-4.md` → противоречащие указания на строках `38` и `46`.

- [NIT] docs/tasks/codex/2026-09-06-final-dev4-stage-4.md:219 — «по шагу 8 карточки» — промпт направляет за спецификацией DOM-гейта в шаг обновления копирайт-контракта; нужная спецификация находится в шаге 9. — `rg -n -o '### [89]\..*|по шагу 8 карточки' docs/tasks/codex/2026-09-06-final-dev4-stage-4.md` → `90:### 8. Обновить контракт копирайта…`; `98:### 9. Добавить DOM-гейт…`; `219:по шагу 8 карточки`.

ПРОВЕРЕНО И ЧИСТО: извлечение промптов через Python из восьми карточек → каждый явно требует прочитать собственную карточку (`own_card=True`); отсутствие деталей в кратком промпте само по себе находкой не считалось. Проверка ссылок через `Path.exists()` → отсутствующие скрипты предусмотрены шагами создания текущего или предыдущих этапов. `git branch -r --list '*final-dev4*'` → `origin/codex/final-dev4-s1-prep` присутствует. `git diff --check` → без вывода.

НЕ ПРОВЕРЕНО: исполнение этапов с изменением файлов, полные сборки, browser matrix, публикация и live-readback — режим только чтения; этапы ещё не реализованы в проверяемом дереве. Шрифтовой конфликт проверен подстановкой в памяти, без браузерного прогона будущей реализации.