SHA: e946b76
ЛИНЗА: source · РАУНД: 4

НАХОДКИ (по убыванию тяжести; severity ∈ BLOCKER | DEFECT | NIT):

- [DEFECT] docs/tasks/codex/2026-09-06-final-dev4-stage-4.md:54 — `data-business-closed="hide"` — карточка предписывает скрывать телефонный ряд у формы, хотя владелец явно выбрал замену на WhatsApp; этот ряд прямо включён в вопрос №25. Решение подтверждено spec:199. Команда: `rg -n -o 'data-business-closed="hide"|Заменить на WhatsApp, как в Hero\.' docs/tasks -g '*stage-4.md' -g '*questions.md'` → stage-4:54,56,102,215: `data-business-closed="hide"`; questions:227: `Заменить на WhatsApp, как в Hero.`

- [DEFECT] docs/tasks/codex/2026-09-06-final-dev4-stage-7.md:25 — «по умолчанию Hero/подвал замеряются и выносятся вопросом, не меняются» — карточка повторно открывает уже решённый вопрос охвата отступов. Владелец включил Hero и подвал; открыты величины. Умолчание `measure-only` исключает их из выравнивания, хотя рабочая модель разрешает сразу применять отступы 80/48. Команда: `rg -n -o 'SPACING_HERO_FOOTER=measure-only[^;]*|для всех секций, включая (Hero и подвал|первый экран и подвал)' docs/tasks -g '*stage-7.md' -g '*questions.md' -g '*spec.md'` → questions:253: `для всех секций, включая Hero и подвал`; spec:202: `для всех секций, включая первый экран и подвал`; stage-7:115: `SPACING_HERO_FOOTER=measure-only (по умолчанию): .site-footer__inner 44/28 не менять`.

ПРОВЕРЕНО И ЧИСТО:

- `python -B -` — посимвольное сравнение колонки «Правка» с литералами owner-контракта карточки этапа 2 → строки 15,18,19,24,27,29,32,34,37,45,46,67: все `True`, без нормализации символов.
- `python -B -` — сравнение заголовков и подзаголовков строк 8–10 с HTML карточки этапа 5 → для каждой `TITLE EXACT True SUB EXACT True`; отделены служебные подписи и `<br>`.
- Снятие `<br>` в строке 26 имеет основание: чтение questions:185–191 через `Get-Content -Encoding UTF8` → ответ владельца «Сделать как принято, best practice»; stage-2:21–23 явно оформляет естественный перенос как умолчание с переделкой при другом ответе.

НЕ ПРОВЕРЕНО: исполняемость команд карточек, сборка, браузерное поведение и live-состояние — вне линзы source. Полная посимвольная сверка всех повторных цитат в items/spec не выполнена; отсутствие остальных дефектов не утверждается.