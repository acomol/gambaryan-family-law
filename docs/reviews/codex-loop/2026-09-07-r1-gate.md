SHA: ca70b9e
ЛИНЗА: gate · РАУНД: 1

НАХОДКИ (по убыванию тяжести; severity ∈ BLOCKER | DEFECT | NIT):

- [DEFECT] scripts/verify-live-previews.py:185 — «Preview отдают текущий релиз; боевой адрес не изменён» — ложный PASS: пустая страница с токенами в комментарии проходит; CSS-переопределения тоже проходят; изменённый production принимается. Проверка не доказывает заявленное. Отдельное сравнение SHA из карточек этим выводом не заменяется. Команда: `python -B -c "import runpy;v=runpy.run_path('scripts/verify-live-previews.py');f=v['check_preview'];q=chr(34);p='<meta content='+q+'noindex'+q+'><link rel='+q+'stylesheet'+q+' href='+q+'styles.css'+q+'><!--'+'&nbsp;—'*23+'-->';c='.nav-links{font-size:14px;}'+('[data-copy-id='+q+'2.10'+q+'] .fact-card__unit{color:#fff;}')*2;f.__globals__['fetch']=lambda u:(200,'text/css',c) if u.endswith('.css') else (200,'text/html',p);print('empty:',f('final-dev3'));c+='.nav-links{font-size:1px!important}.fact-card__unit{color:red!important}';print('overridden CSS:',f('final-dev3'));p='<meta content='+q+'noindex'+q+'><h1>Changed</h1>';print('production:',v['check_production']())"` → `empty: []; overridden CSS: []; production: []`.

- [DEFECT] scripts/verify-client-copy.py:197 — «def handle_startendtag(» — самозакрывающийся `<input … />` обходит сбор текстовых атрибутов. Неутверждённый placeholder пропущен; при мутации текущего HTML полный `verify_html` также вернул `[]`, тогда как обычный `<input …>` дал ошибку. Команда: `python -B -c "import runpy,sys;sys.path.insert(0,'scripts');v=runpy.run_path('scripts/verify-client-copy.py');p=v['CopyHTMLParser']();q=chr(34);p.feed('<input placeholder='+q+'Гарантируем победу в суде'+q+' />');print(p.text_attributes)"` → `()`.

- [DEFECT] scripts/verify-client-copy.py:316 — «if CYRILLIC_RE.search(match.group(2))» — ложный PASS для динамического текста через Unicode escapes: проверяется исходная запись строки без декодирования. Та же фраза обычной кириллицей отвергается. Команда: `python -B -c "import runpy,sys;from unittest.mock import Mock;sys.path.insert(0,'scripts');v=runpy.run_path('scripts/verify-client-copy.py');p=Mock();p.is_file.return_value=True;p.read_text.return_value='const claim = '+chr(34)+''.join(chr(92)+'u%04x'%ord(c) for c in 'Гарантируем победу в суде')+chr(34)+';';print(v['verify_dynamic_ui']((p,)))"` → `[]`.

- [DEFECT] scripts/verify-client-copy.py:292 — «r'<script\s+type=…'» — ложный FAIL: добавление `id` перед `type` скрывает корректный JSON-LD от регулярки. Содержимое объекта Юлии не меняется, но полный verifier сообщает «JSON-LD Юлии не совпадает». Аналогичная регулярка на строке 252 пропускает проверку текстов такого блока. Команда: `python -B -c "import runpy,sys;from pathlib import Path;sys.path.insert(0,'scripts');v=runpy.run_path('scripts/verify-client-copy.py');h=Path('site/index.html').read_text(encoding='utf-8');q=chr(34);print(v['owner_json_ld_count'](h));h=h.replace('<script type='+q+'application/ld+json'+q+'>','<script id='+q+'schema'+q+' type='+q+'application/ld+json'+q+'>');print(v['owner_json_ld_count'](h))"` → `1`, затем `0`.

- [DEFECT] docs/tasks/codex/2026-09-06-final-dev4-stage-7.md:109 — «grep -n 'aspect-ratio' site/styles.css | grep -c 'attorney' → 1» — ложный FAIL для корректного многострочного CSS: селектор и декларация находятся на разных строках. Команда PowerShell: `".attorney-photo {`n  aspect-ratio: 4 / 3;`n}" | grep -n 'aspect-ratio' | grep -c 'attorney'` → `0`, exit `1`.

ПРОВЕРЕНО И ЧИСТО:

- `python -B -c "import runpy,sys;from pathlib import Path;sys.path.insert(0,'scripts');v=runpy.run_path('scripts/verify-client-copy.py');print('frozen:',v['verify_frozen_source']());print('source:',v['verify_html'](Path('site/index.html')))"` → `frozen: []; source: []`.
- Через `python -B -` выполнены 10 существующих unit-тестов с заменой временных файлов объектами в памяти → `Ran 10 tests … OK`. Мутации утверждённого текста, структуры Юлии, обычных атрибутов и запрещённых полей обнаружены.
- Добавление байта `x` к frozen source в памяти → ошибка SHA-256. Подмена ответа CSS на `text/html` через мок `fetch` → `styles.css … soft-404`.
- `git status --short` до/после одинаков: только два исходных untracked task-файла.

НЕ ПРОВЕРЕНО: полный запуск сборщиков, файлового unittest-suite, browser matrix и живых URL. Режим только чтения; мутации выполнялись в памяти. `verify-address-links.py` и `verify-business-hours.py` пока отсутствуют и предусмотрены будущими этапами: их поведение и негативные тесты не проверены. Для всех команд карточек исчерпывающий набор контрпримеров не выполнен.