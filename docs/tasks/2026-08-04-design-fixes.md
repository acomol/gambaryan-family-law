# Задание: дизайн-фиксы по аудиту 2026-08-04

> Исполнитель: Codex. Приёмка: ведущий агент сессии, по секции «Приёмка»
> этого файла. Композиционная рамка: `docs/SCREEN-COMPOSITION.md`.
> Ветка: `claude/website-development-kb0fu0`. Область: только `site/`.

## Правила (нарушение = возврат на доработку)

1. **Не менять**: семейства шрифтов (Onest + Playfair Display — свежее
   решение), отсутствие глобального `box-sizing: border-box` (намеренно,
   см. шапку styles.css), любые решения, задокументированные комментариями
   в CSS — комментарии там нормативные.
2. Цвета — только из токенов `:root`; новые hex не заводить.
3. Каждый блок — отдельный коммит (`fix:` для A, `refactor:` для B,
   `chore:`/`refactor:` для C) с шаблонным футером репозитория.
4. После каждого блока — скриншоты 390 и 1280 затронутых мест.

## Блок A — видимые дефекты и доступность (P1)

| № | Где | Что сделать | Критерий приёмки |
|---|-----|-------------|------------------|
| A1 | `styles.css` (правила нет), элемент `index.html:516` | Добавить `.lead-form__error { color: var(--wine); font-size: 13.5px; }`, ссылке внутри — `color: var(--wine); text-decoration: underline` | Текст ошибки читаем на белой карточке; скриншот состояния error |
| A2 | `styles.css:730` | В существующем `@media (max-width:1200px)` сбросить `margin-top` `.precedent-card` до `clamp(40px, 6vw, 64px)` | На 390 и 768 между секцией услуг и карточкой нет чёрной паузы ~176px |
| A3 | `styles.css:68-75` | Фокус-индикатор двухслойный: `outline: 2px solid var(--gold); box-shadow: 0 0 0 4px rgba(20,25,31,.9)` — проверить на кремовом фоне и на золотых кнопках | Фокус различим на #f6f1e8 и на `.btn--gold-block` (скриншот) |
| A4 | `styles.css:1061-1067` | `.lead-form__legal`: `--ink-4` → `--ink-3` | Контраст ≥ 4.5:1 |
| A5 | `styles.css:1155-1160` | `.site-footer__legal`: `rgba(255,255,255,.4)` → `.55` | Контраст ≥ 4.5:1 |
| A6 | `styles.css` | Добавить `[id] { scroll-margin-top: 90px; }` | Клик по каждому пункту навигации показывает надстрочник секции целиком под шапкой (390 и 1280) |

## Блок B — согласованность системы (P2)

| № | Где | Что сделать | Критерий приёмки |
|---|-----|-------------|------------------|
| B1 | `styles.css:1040` | Убрать `outline: none` у `.lead-form input`; фокус: рамка `2px var(--gold)` + `box-shadow: 0 0 0 3px rgba(138,31,31,.25)` | Фокус поля различим (контраст индикатора ≥ 3:1) |
| B2 | радиусы кнопок | Свести к двум ступеням: 8px (обычные), 12px (крупные gold-lg/ghost-lg); `.btn--gold-block` 6→8, `.svc-card__cta` 10→8 | В `report.json`-стиле замер: только {8,12} у кнопок |
| B3 | `styles.css:637-646` | `.svc-card__cta` перевести на базу `.btn` (или выровнять высоту/падинги с `.btn--wine`) | Высота совпадает с `.btn--wine` ±1px |
| B4 | `styles.css:818/830` | `.btn--ghost-lg { height: 50px; }` (content-box + 2×1px рамка = 52) | Обе кнопки стека прецедента 52px |
| B5 | надстрочники | Две ступени: секционные 11px/0.22em, карточечные 10.5px/0.2em; `logo__sub` не трогать | Не более двух пар размер/трекинг у надстрочников (кроме логотипа) |
| B6 | `styles.css:659-666` | `.svc-media__label`: alpha `.45` → `.55` | Контраст ≥ 4.5:1 |
| B7 | `styles.css:555-565, 704-714` | Hover у `.svc-tab` и `.svc-dot` + transition как у `.svc-arrow` | Все интерактивы карусели имеют hover |
| B8 | `styles.css:436-441` | В диапазоне 2 колонок: `.fact-card:last-child:nth-child(odd) { grid-column: 1 / -1; }` | На 768 нет карточки-сироты на полширины |
| ~~B9~~ | hero mobile | **Сделано ведущим агентом 2026-08-04** (по прямому запросу владельца): надстрочник скрыт на мобильном, отступы полос ужаты, CTA виден целиком на 390×740 (замер: top 715→644, видимость 25/52→52/52px). Не переделывать. | — |

## Блок C — полировка (P3)

| № | Где | Что сделать |
|---|-----|-------------|
| C1 | `app.js:46-126` | Удалить мёртвый блок слайдера (`.hero-slide` в разметке нет) |
| C2 | `index.html:467` | Убрать осиротевший класс `attorney-photo--yulia` |
| C3 | `styles.css:403/731/820` | Цвета на токены: gold-lg `color: var(--bg)`; hero__lede — rgba от `--bg-cream`; градиент прецедента — комментарий о намеренности |
| C4 | `styles.css:134-142` | `.section-title { text-wrap: balance; }` |
| C5 | `styles.css:1086-1093` | `.form-success__title { line-height: 1.25; }` |
| C6 | `styles.css:727` | Нижний отступ прецедента → `var(--section-pad)` |
| C7 | `styles.css:77` | Плейсхолдеры `--ink-4` → `--ink-3` |
| C8 | `index.html` | SVG-иконки: `stroke="#F0AE1F"` → `stroke="currentColor"` + цвет на контейнере |

Критерий для всего блока C: нулевая визуальная регрессия на скриншотах
390/1280 (кроме целевых мест C4, C5).

## Решения владельца (2026-08-04, зафиксированы)

1. **Опыт — «Более 30 лет» везде.** Hero исправлен ведущим агентом
   (коммит в этой же ветке); при любых правках текста цифру 24 не
   возвращать.
2. **Порядок карточек фактов НЕ менять.** Владелец: контент берётся из
   утверждённых клиентом материалов как есть.

## Правило контента (нарушение = возврат)

Тексты на странице — только из утверждённых клиентом материалов
(страница первой версии и существующая разметка). Не переформулировать,
не сокращать, не добавлять новые формулировки. Любое изменение текста,
кроме зафиксированных выше, — только после подтверждения владельца
через ведущего агента.

## Приёмка (прогон целиком после всех блоков)

1. Скриншоты 360/390/768/1024/1280/1600 — глазами.
2. `scrollWidth === innerWidth` на каждой ширине.
3. Прогон состояний: бургер, фокус всех типов кнопок на обоих фонах,
   форма (focus / success / error).
4. Контраст-пары из блоков A и B — пересчитать фактические значения.
5. `CSS.getPlatformFontsForNode`: только Onest и Playfair Display.
6. Критерий первого экрана из `SCREEN-COMPOSITION.md` (390×740).
7. Proof-блок: commit-хеши + пуш подтверждён.
