# Этап 7: Шрифты, фото, отступы: заголовки без засечек, равные кадры адвокатов, единый ряд отступов (параметрический этап)

**Версия:** `FINAL-DEV4-STAGE-7 v1.0.0`

**Дата:** `2026-09-06`

**Ветка:** `codex/final-dev4-s7-fonts-photos-spacing` → draft PR в `main`

**Исполнитель:** Codex CLI; уровень рассуждений: **high (контракты, скрипты, поведение)** — Этап пишет четыре новых Playwright/PIL-скрипта, которые становятся гейтами приёмки (шрифты по CDP, геометрия секций, детектор верха головы, кадрирование с выравниванием масштаба), меняет физические шрифтовые файлы и их контракт (cmap, preload, standalone-дубликат, EXPECTED_FONTS), переписывает дизайн-контракт (major в GAMBARIAN-DESIGN-RULES) и токены отступов, задевающие все Preview; половина решений параметрическая и должна одинаково корректно исполниться при любом ответе владельца — это уровень «контракты, скрипты, поведение», а не механическая правка.

**Приёмка:** Claude (облачная сессия) по `docs/CODEX-WORKING-MODEL.md`; слияние — владелец.

## Цель

Ветка codex/final-dev4-s7-fonts-photos-spacing поверх результата этапов 1–6. Часть A (по умолчанию, делать сразу): три закоммиченных скрипта замера (scripts/measure-fonts.py — CDP CSS.getPlatformFontsForNode, «0 фолбэков» на 1440/390; scripts/measure-section-gaps.py — пустоты по границам секций по методу SPACING-MEASUREMENTS; scripts/measure-head-top.py — верх головы по методу FONT-AND-PHOTO-MEASUREMENTS), таблицы «до/после» в docs; фото адвокатов перекадрированы из исходников docs/source-photos/ скриптом scripts/crop-attorney-cards.py под один кадр PHOTO_RATIO из реестра решений (при пустом PHOTO_RATIO пропорция окна 4/5 не меняется — только выравнивание кадров; при 4:3 высота фото ≈418px на 1440): один размер, один масштаб лица, верх головы на одной высоте (разница ≤3px на 1440 и 390), фото Юлии не растягивается и не дорисовывается, мобильные компенсации object-position и 16/15 сняты; отступы приведены к одному токену --section-pad = clamp(GAP_MOBILE, 8vw, GAP_DESKTOP) = 48/80 для контентных секций, прецедент переведён на тот же токен (закрывает C6), запас над карточкой прецедента на desktop 187 → PRECEDENT_RESERVE=80, min-height Hero не меняется; Hero и подвал только замеряются и выносятся вопросом. Часть B (только при заполненных FONT_HEADINGS и EXTEND_UNMARKED из реестра решений): Playfair Display уходит из всех 13 помеченных заголовков (и, при EXTEND_UNMARKED=yes, из всех остальных потребителей --font-serif), курсив снимается, иерархия — толщиной (H1 700 / H2 700 / H3 600), физические woff2-поднаборы семейства заголовков скачаны тем же механизмом, что build-font-variants.py (Google Fonts css2 → cmap-проверка кириллицы → site/fonts/ с хэшем), fonts.css, preload в index.html и standalone синхронизированы, EXPECTED_FONTS/RUNNER_VERSION обновлены, замер «0 фолбэков» на 1440 и 390 приложен. На выходе build/variants/final-dev4 проходит все гейты, эталоны переотсняты, маркеры версий синхронизированы.

## Основание

- Решения владельца (реестр в `../2026-09-06-final-dev4-spec.md`): №1–2 (частично: без засечек, курсив снимается, иерархия толщиной), №3 (частично: одинаковые, верх головы ≤3px, перекадрирование исходников, Юлию не растягивать), №28 (частично: одинаковые отступы; величины — по умолчанию 80/48, запас 80, min-height оставить), №9
- Пункты разбора (`../2026-09-06-final-dev4-items.md`): `E:G-01`, `E:G-02`, `B:G-05`, `C:G-04`, `D:D-01`, `A:HF-01`, `E:G-07`, `B:G-13`, `E:G-04`, `E:G-05`, `A:HF-08`
- Открытые вопросы, выполняемые «по умолчанию» (переделать при другом ответе):
  - GAP_DESKTOP=80, GAP_MOBILE=48 (CODEX-WORKING-MODEL «Открытые вопросы»: «отступы 80/48» можно делать сразу; рекомендация №28)
  - PRECEDENT_RESERVE=80 на desktop >1200 (там же: «запас 80» = 48 выступ портрета + 32 воздуха)
  - HERO_MINH=keep — min-height Hero остаётся (рекомендация №28: кадр на широких мониторах, макушки уже ловили как дефект)
  - SPACING_HERO_FOOTER=measure-only — владелец ответил «для всех секций, включая первый экран и подвал», но величины открыты; по умолчанию Hero/подвал замеряются и выносятся вопросом, не меняются
  - HEADING_LIGHT=none — тонкие подзаголовки не вводятся до ответа, какие элементы считать подзаголовками; веса Onest 400–800 уже на месте, новых файлов для варианта onest не нужно
  - Схема весов заголовков при Части B (рекомендация №1): .hero__title 700; .section-title 700; .svc-title, .precedent-card__title, .attorney-card__name 600; при EXTEND_UNMARKED=yes .svc-media__name 600, .form-success__title 600, заголовки кубиков (селекторы этапа 5) 600; font-style normal везде
  - Имя CSS-токена --font-serif сохраняется (значение меняется на семейство заголовков): build-font-variants.py подменяет токены по имени, переименование сломало бы сборку v1–v4
  - Новые ассеты называются alexander-card-v3-* / yulia-card-v3-* по образцу существующих имён (<name>-<width>w.<8 hex>.{jpg,webp}); ширины 480/760/1100

## Не в скоупе этапа

- Выбор семейства без засечек (№1) и распространение на заголовки без пометки (№2) — не решать самостоятельно: Часть B выполняется только с заполненными FONT_HEADINGS/EXTEND_UNMARKED; без них Часть B пропускается целиком и помечается «отложено: ждёт ответа владельца»
- Блок «Ведёт» для Юлии (№18, B:G-13) — данных нет; готовится только общий перекадрированный источник yulia-card-v3, аватар yulia-avatar-* не собирать, разметку .svc-media не менять
- Подзаголовки тонким начертанием (HEADING_LIGHT=300) — без ответа владельца, какие именно подзаголовки, не делать; веса основного текста, .eyebrow, кнопок, .attorney-card__role не трогать
- Паддинги Hero (живут в FINAL-DEV1-HERO builder-CSS, короткий mobile-гейт CTA) и подвала (44/28 намеренно плотнее) — при SPACING_HERO_FOOTER=measure-only только замер и вопрос владельцу; min-height Hero clamp(560px,84vh,880px) не менять
- Внутренние отступы вне ряда №28: .rule margin-bottom 40, .services__head 34, .svc-tabs 26, .attorneys__note 32, подпись формы 26 — не трогать (SPACING-MEASUREMENTS п.4 — предложение, не решение)
- Кремовое поле фактов 32/20 (этап 5, №10) — не пересчитывать; исключение в гейте отступов
- Тексты, контракт копирайта кроме токенов src/srcset карточки Юлии, JSON-LD, alt-тексты, Action Bar, site-addons/final-dev3, final-dev3, production, wrangler, поле only пустым
- Hero-фото (duo-wide/duo-tall, grade-hero-photo.py, extend-hero-canvas.py) и портрет прецедента — не пересобирать
- Runner qa-browser-matrix 1.5.0 гейты этапа 6 (свайп, табы, toggle-missing) — не переписывать; только EXPECTED_FONTS/роли/версия в Части B
- Font-variants v1–v4 (build/font-variants) — пересобираются builder-ом как есть; их шрифтовые наборы не менять
- Смена пропорции фото (4:3 / 1:1) и CSS aspect-ratio карточек — только при заполненном PHOTO_RATIO из реестра решений (открытый №3 без умолчания в таблице spec); при пустом PHOTO_RATIO выполняется только решённая часть №3: одинаковый кадр, верх головы ≤3px, перекадрирование Александра под текущее окно 4/5, Юлию не трогать; мобильные компенсации 16/15 и center 16% снимаются только если замер это подтверждает

## Шаги

### 1. Создать ветку от main после слияния этапа 6, проверить предпосылки и зафиксировать параметры этапа.

Файлы (только чтение, шаг ничего не меняет): `docs/tasks/2026-09-06-final-dev4-spec.md`, `scripts/qa-browser-matrix.py`, `scripts/final_dev4_contract.py`, `scripts/client_copy_contract.py`, `site/styles.css`

git checkout main && git pull --ff-only && git checkout -b codex/final-dev4-s7-fonts-photos-spacing. Прочитать AGENTS.md → docs/RESUME.md → docs/CODEX-WORKING-MODEL.md → эту карточку → spec: «Реестр решений владельца» (№1–2, 3, 28, 9), «Открытые вопросы (9) и значения по умолчанию», «Подготовительные шаги» п.6–7, «Правила для исполнителя» → items E:G-01, E:G-02, B:G-05, C:G-04, D:D-01, E:G-07, B:G-13, E:G-04, E:G-05 → DESIGN-RECOMMENDATIONS №1, №2, №3, №28 → SPACING-MEASUREMENTS и FONT-AND-PHOTO-MEASUREMENTS целиком → docs/source-photos/README.md → scripts/build-font-variants.py (fetch/parse_css/cyrillic_glyphs — механизм скачивания и проверки cmap). Предпосылки: RUNNER_VERSION в qa-browser-matrix.py = 1.5.0 (этап 6); кубики перестроены (в site/styles.css нет .fact-card__num; grep 'var(--font-serif)' даёт актуальный список потребителей); alias final-dev4 в карте; CONTRACT_VERSION 1.4.x (после этапа 5). Параметры взять ТОЛЬКО из реестра решений spec (архитектор вписывает их в блок «Параметры» промпта): FONT_HEADINGS ∈ {onest, golos, manrope} | пусто; EXTEND_UNMARKED ∈ {yes, no} | пусто; HEADING_LIGHT ∈ {none, 300} (по умолчанию none); PHOTO_RATIO ∈ {4:3, 1:1} | пусто (пусто = пропорцию не менять, только выравнивание в окне 4/5); GAP_DESKTOP (80), GAP_MOBILE (48), PRECEDENT_RESERVE (80), HERO_MINH (keep), SPACING_HERO_FOOTER ∈ {measure-only, align} (по умолчанию measure-only). Если FONT_HEADINGS или EXTEND_UNMARKED пусты — Часть B (шаги 10–12) не выполнять, в отчёте «отложено: ждёт ответа №1/№2». Установить зависимости: pip -r requirements-build.txt, playwright install chromium, npm ci. Собрать текущее состояние: python -B scripts/build-preview.py site/gambarian-standalone.html --standalone && python -B scripts/build-hero-variants.py dev4 — это база для замера «до».

Проверка: git branch --show-current → codex/final-dev4-s7-fonts-photos-spacing; grep -n 'RUNNER_VERSION = ' scripts/qa-browser-matrix.py → 1.5.0; grep -c 'fact-card__num' site/styles.css → 0; test -f build/variants/final-dev4/index.html; блок параметров с явными значениями записан в первую строку отчёта

### 2. Добавить скрипт замера реально отрисованных шрифтов (подготовительный шаг 7 spec).

Файлы: `scripts/measure-fonts.py (новый)`

Маркер в docstring: FONT-MEASURE v1.0.0 | <ДАТА>. Аргументы: base_url (локальный http://127.0.0.1:8098/build/variants/final-dev4/ или живой https://final-dev4.gambarian-landing.pages.dev/), --viewports по умолчанию 1440x900,390x844, --json <файл>, --expect-heading <семейство> (необязательно), --selectors (по умолчанию .hero__title,.section-title,.svc-title,.precedent-card__title,.attorney-card__name,.svc-media__name,.form-success__title плюс заголовки кубиков этапа 5). Метод из docs/FONT-AND-PHOTO-MEASUREMENTS-2026-09-06.md §«Шрифты — что реально отрисовано»: Playwright Chromium (executable_path из PLAYWRIGHT_BROWSERS_PATH, если задан — spec шаг 7), дождаться document.fonts.ready и принудительно загрузить ленивые изображения, затем CDP DOM.getDocument(depth -1, pierce) + CSS.getPlatformFontsForNode для КАЖДОГО элемента с непустым прямым текстовым узлом (обход через DOM.querySelectorAll('body *') и DOM.describeNode/DOM.getOuterHTML — или проще: page.evaluate собирает уникальные CSS-пути элементов с прямым текстом, для каждого DOM.querySelector → nodeId). Считать по каждому семейству сумму glyphCount, долю, число элементов, у которых хотя бы один шрифт isCustomFont=false с glyphCount>0 (= фолбэк). Для --selectors дополнительно: computed fontFamily (первое семейство), fontStyle, fontWeight, dominant postScriptName и признак «faux»: fontStyle==='italic' при отсутствии italic-@font-face этого семейства в fonts.css, или fontWeight вне диапазонов font-weight @font-face семейства (парсить fonts.css со страницы). Вывод: таблица markdown в stdout (как в документе: вьюпорт / шрифт / тип / глифов / доля / элементов с фолбэком) + JSON. Exit 1, если на любом вьюпорте элементов с фолбэком > 0, есть faux, или --expect-heading задан и хотя бы один селектор из списка отрисован не этим семейством; иначе 0. Без сети во время замера (внешних запросов шрифтов быть не должно — проверять page.on('request') на fonts.g*/woff).

Проверка: python -m http.server 8098 (из корня, фон) && python scripts/measure-fonts.py http://127.0.0.1:8098/build/variants/final-dev4/ --json /tmp/fonts-before.json → на текущей сборке exit 0, таблица показывает Onest ≈89–90% и Playfair Display ≈10–11%, элементов с фолбэком 0 на обоих вьюпортах (сходится с FONT-AND-PHOTO-MEASUREMENTS); python scripts/measure-fonts.py <url> --expect-heading 'Onest' → exit 1 с перечнем Playfair-селекторов (гейт работает)

### 3. Добавить скрипт замера пустот по границам секций (метод SPACING-MEASUREMENTS).

Файлы: `scripts/measure-section-gaps.py (новый)`

Маркер SECTION-GAPS-MEASURE v1.0.0 | <ДАТА>. Аргументы: base_url, --viewports по умолчанию 1440x900,1440x1200,390x844,390x740, --json, --baseline <json> (для колонки «до»), --expect-desktop/--expect-mobile (px), --tolerance 4, --allow (список секций-исключений, по умолчанию hero,facts,footer), --annotate <каталог> (сохранить скриншоты с разметкой: пустота сверху красным, снизу синим, у правого края — как spacing-2026-09-06-*.jpg). Метод из docs/SPACING-MEASUREMENTS-2026-09-06.md §«Метод»: ждать document.fonts.ready, принудительно загрузить lazy-изображения, для каждой секции header/main > section/footer (id или класс как имя): высота, «пусто сверху» = top первого видимого контентного бокса (текст, img, кнопка; элементы с getClientRects и непустым текстом или img) − top секции, «пусто снизу» = bottom секции − bottom последнего контентного бокса; отдельно для .precedent: top портрета .precedent-photo относительно низа предыдущей секции (выступ/наезд) и отступ до карточки; между соседними секциями: расстояние между последним контентом N и первым контентом N+1. Ширины 1201 и 1280 добавить флагом --viewports для проверки наезда портрета. Вывод: таблица markdown в формате документа (Секция / Высота / Пусто сверху / Пусто снизу / Внутри) и «до → после», если задан --baseline; JSON. Exit 1, если при заданных --expect-* у секции не из --allow верх или низ отличается от ожидания больше --tolerance, или портрет прецедента верхом заходит выше низа секции услуг более чем на 0px (наезд); иначе 0.

Проверка: python scripts/measure-section-gaps.py http://127.0.0.1:8098/build/variants/final-dev4/ --json /tmp/gaps-before.json --annotate /tmp/gaps-before → exit 0 без --expect; таблица 1440 воспроизводит порядок величин документа для неизменённых секций (услуги 96/96, адвокаты 96/96, консультация 96/96, подвал 45/28), факты — значения этапа 5 (32/32); python scripts/measure-section-gaps.py <url> --expect-desktop 80 --expect-mobile 48 → exit 1 на текущей сборке (гейт различает 96 и 80)

### 4. Добавить скрипт замера верха головы на карточках адвокатов (метод FONT-AND-PHOTO-MEASUREMENTS).

Файлы: `scripts/measure-head-top.py (новый)`

Маркер HEAD-TOP-MEASURE v1.0.0 | <ДАТА>. Два режима. (1) --url base_url: Playwright, viewports 1440x900 и 390x844, скрыть фиксированную шапку (header.site-header → visibility:hidden через addStyleTag; иначе desktop-значения искажаются — документ §«Фотографии адвокатов», абзац о повторном замере), проскроллить каждую img.attorney-photo в кадр, дождаться загрузки (naturalWidth>0, decode()), page.screenshot(clip по getBoundingClientRect) → PNG. (2) --files a.jpg b.jpg: те же файлы напрямую (для проверки кропов до сборки страницы). Детектор из документа: центральная полоса 30–70% ширины; фон строки — медиана крайних 10% пикселей строки; «верх головы» = первая строка, где сумма |ΔRGB| относительно фона строки превышает 90 хотя бы у 3% пикселей полосы, и так 4 строки подряд. Вывод: для каждого кадра px и % высоты, разница между двумя карточками на каждом вьюпорте, размеченные PNG (горизонтальная линия на найденной строке) в --out <каталог> с именами attorney-head-top-<ДАТА>-<viewport>-<0|1>-marked.png. JSON через --json. Exit 1, если разница верха головы > --max-diff (по умолчанию 3px) на любом вьюпорте; иначе 0. Отметки проверить глазами, как требует документ.

Проверка: python scripts/measure-head-top.py --url http://127.0.0.1:8098/build/variants/final-dev4/ --out /tmp/head-before --json /tmp/head-before.json → exit 1 на текущей сборке с разницей ≈45px на 1440 (75 vs 30) и ≈18px на 390 (38 vs 20) — сходится с документом; python scripts/measure-head-top.py --files docs/source-photos/alexander-portrait.jpg docs/source-photos/yulia-portrait.jpg → верх головы ≈9.0% и ≈1.3%

### 5. Снять замер «до» на сборке после этапа 6 и сохранить его в репозиторий.

Файлы: `docs/design-references/measurements/<ДАТА>-fonts-before.json (новый)`, `docs/design-references/measurements/<ДАТА>-gaps-before.json (новый)`, `docs/design-references/measurements/<ДАТА>-head-before.json (новый)`

Прогнать три скрипта шагов 2–4 по http://127.0.0.1:8098/build/variants/final-dev4/ (сборка из шага 1, до любых правок этого этапа) с --json в docs/design-references/measurements/ (новый каталог). Размеченные PNG/JPG «до» в репозиторий не класть (объём) — только JSON; в PR приложить как вложения. Эти файлы — единственный источник колонки «до» в таблицах шага 14; менять их после правок нельзя.

Проверка: ls docs/design-references/measurements/ → три файла *-before.json; python -c "import json;print(json.load(open('docs/design-references/measurements/<ДАТА>-gaps-before.json'))['viewports'][0]['sections'][:2])" печатает hero и facts

### 6. Перекадрировать карточки адвокатов из исходников под один кадр PHOTO_RATIO скриптом кадрирования; закрепить Pillow.

Файлы: `scripts/crop-attorney-cards.py (новый)`, `requirements-build.txt`, `site/assets/manifest.json`, `docs/source-photos/README.md`, `site/assets/ (новые alexander-card-v3-*, yulia-card-v3-*)`

requirements-build.txt: добавить Pillow==<текущая установленная версия> (grade-hero-photo.py уже импортирует PIL, но зависимость не закреплена), маркер BUILD-TOOLS 1.1.1 → 1.2.0 | <ДАТА>. Скрипт (маркер ATTORNEY-CARD-CROP v1.0.0 | <ДАТА>): входы docs/source-photos/alexander-portrait.jpg (2794×4284) и yulia-portrait.jpg (1122×1402); параметры --ratio 4:3|1:1 (PHOTO_RATIO), --widths 480,760,1100, --version v3, --quality 82, --face (JSON с двумя опорными точками на каждого: head_top — берётся автоматически детектором measure-head-top.py по файлу, chin — координата подбородка, вводится вручную с размеченных кадров docs/design-references/attorney-head-top-2026-09-06-desktop-*-clean-marked.png и исходников, записывается в docs/source-photos/README.md как источник числа). Алгоритм: опорный кадр — Юлия (запаса над головой почти нет, растягивать/дорисовывать запрещено решением №3): ширина = полная 1122, высота = 1122/ratio, top = 0 (при ratio 1:1 высота 1122 — проверить, что низ не режет по локтям: см. оговорку №3 п.4); доля верха головы h = head_top_Y/height_Y и масштаб лица f = (chin_Y − head_top_Y)/height_Y. Кадр Александра: height_A = (chin_A − head_top_A)/f; width_A = height_A×ratio; top_A = head_top_A − h×height_A; left_A = центр лица по X (середина полосы 30–70%, где найдена голова) − width_A/2; если кадр выходит за границы исходника — уменьшить масштаб ровно настолько, чтобы влезть, и напечатать предупреждение с фактическим отклонением (при выходе за 3px замера — остановиться, вопрос в отчёт). Ресемплинг LANCZOS в ширины 480/760/1100 (высота по ratio), jpg (quality 82, progressive) и webp (quality 82); имена <person>-card-v3-<w>w.<первые 8 hex sha256 файла>.{jpg,webp}; старые alexander-card-v2-* и yulia-card-* удалить из site/assets только после замены ссылок (шаг 7) — git rm. manifest.json: записи alexander-card-v3 и yulia-card-v3 по образцу yulia-card (alt, natural = размер кропа в исходнике, fallbackExt, variants с байтами); старые записи alexander-card/yulia-card удалить (alexander-card 2100×2200 и так не используется), alexander-avatar оставить. README.md docs/source-photos: раздел «Пайплайн карточек адвокатов» (команда, параметры, откуда взяты chin, где эталоны замера), таблица «Где используется» → alexander-card-v3 / yulia-card-v3. Проверить кропы до сборки: python scripts/measure-head-top.py --files <1100w jpg обоих> → разница верха головы в % ≤ 3px при высоте 418 (на 1440 карточка 558px шириной → высота 418.5 при 4:3), лица не срезаны, у Юлии кадр = ширина исходника без масштабирования вверх.

Проверка: ls site/assets | grep -c 'card-v3' → 12 (2 персоны × 3 ширины × 2 формата); python -c "from PIL import Image;import glob;print({f:Image.open(f).size for f in sorted(glob.glob('site/assets/*-card-v3-1100w.*.jpg'))})" → при PHOTO_RATIO=4:3 — 1100×825 у обоих, при 1:1 — 1100×1100, при пустом — 1100×1375 (окно 4/5 без изменений); python scripts/measure-head-top.py --files site/assets/alexander-card-v3-1100w.*.jpg site/assets/yulia-card-v3-1100w.*.jpg --max-diff 6 → exit 0 (6px на 1100 ≈ 3px на 558); grep -c 'card-v3' site/assets/manifest.json → 2; grep -c 'Pillow' requirements-build.txt → 1

### 7. Переключить разметку карточек на новые ассеты и синхронизировать все места, где имена файлов зашиты.

Файлы: `site/index.html`, `scripts/client_copy_contract.py`, `scripts/tests/test_verify_client_copy.py`, `scripts/deploy-pages.sh`, `scripts/deploy-pages.ps1`, `docs/RESUME.md`, `docs/FINAL-QA-CHECKLIST.md`, `docs/boards/2026-08-06-versions-links.md`, `docs/CONTENT-SOURCE-MAP.md`, `docs/CONTENT-EXTRA.md`, `docs/tasks/2026-08-13-dark-fact-cards.md`, `docs/CLIENT-PREVIEW-HANDOFF.md`

site/index.html, обе карточки (#attorney): source/srcset и img src/srcset → alexander-card-v3-* / yulia-card-v3-* с теми же дескрипторами 480w/760w/1100w и тем же sizes="(max-width: 787px) calc(100vw - 76px), 534px"; width/height img → 1100 и 825 (натуральные размеры 1100w-варианта; при 1:1 — 1100/1100); у img Юлии снять orphan-класс attorney-photo--yulia (QA C2), остальные атрибуты (alt, loading, decoding) не менять. scripts/client_copy_contract.py OWNER_APPROVED_HTML_TOKENS: токены 'src="assets/yulia-card-760w.df9bd223.jpg"' и srcset webp → новые имена v3 (ровно 1× в каждом target; в standalone src/srcset пропускаются верификатором); CONTRACT_VERSION — следующий patch после этапа 6, CONTRACT_DATE <ДАТА>, docstring :3 и все документы с маркером (grep -rn 'CLIENT-COPY-CONTRACT v\|Client Copy contract' docs scripts). Тест test_owner_approved_yulia_structure_fails: фикстуры src → yulia-card-v3-760w.<hash>.jpg и alexander-card-v3-760w.<hash>.jpg. scripts/deploy-pages.sh (check "alexander-card-v2") и deploy-pages.ps1 (@{ Text = "alexander-card-v2" …}) → "alexander-card-v3" (production-readback; сам production не деплоится). git rm старых alexander-card-v2-* и yulia-card-* (6+6 файлов) после того, как grep по site/ даёт 0.

Проверка: grep -c 'card-v3' site/index.html → 4 (2 source + 2 img; в srcset по 3 вхождения каждый — итог по строкам 4); grep -c 'alexander-card-v2\|yulia-card-[0-9]' site/index.html → 0; grep -c 'attorney-photo--yulia' site/index.html → 0; ls site/assets | grep -c 'alexander-card-v2\|^yulia-card-' → 0; python -B scripts/verify-client-copy.py → PASS (после пересборки standalone в шаге 14); python -m unittest discover -s scripts/tests → OK; grep -n 'card-v3' scripts/deploy-pages.sh scripts/deploy-pages.ps1 → по 1

### 8. CSS карточек: одно окно PHOTO_RATIO на всех ширинах, мобильные компенсации сняты.

Файлы: `site/styles.css`

.attorney-photo (≈:1031–1044): при заполненном PHOTO_RATIO aspect-ratio 4 / 5 → 4 / 3 (или 1 / 1 при PHOTO_RATIO=1:1), при пустом — 4 / 5 остаётся; object-fit cover и object-position center top оставить (при совпадении пропорции кропа и окна cover ничего не режет — комментарий переписать под новый кадр: «окно повторяет пропорцию кропа v3 4:3; выравнивание голов и масштаб заложены в кадре скриптом crop-attorney-cards.py, а не в object-position»). В блоке @media (max-width: 860px) удалить правило .attorney-photo { aspect-ratio: 16 / 15; } с комментарием о решении 2026-08-10 и правило .attorney-card:first-child .attorney-photo { object-position: center 16%; } с комментарием (компенсация больше не нужна: разница верха головы устранена в исходниках; решение 2026-08-10 «ниже на четверть» заменяется решением №3 2026-09-06 — зафиксировать в docs шага 14). Сетка .attorneys-grid и .attorney-card не менять.

Проверка: grep -n 'aspect-ratio' site/styles.css | grep -c 'attorney' → 1; grep -c '16 / 15\|center 16%' site/styles.css → 0; после сборки: python scripts/measure-head-top.py --url http://127.0.0.1:8098/build/variants/final-dev4/ --out docs/design-references/ --json docs/design-references/measurements/<ДАТА>-head-after.json → exit 0, разница ≤3px на 1440 и 390; высота img на 1440 ≈418px

### 9. Отступы: один токен для контентных секций, прецедент на том же токене, запас над карточкой прецедента, Hero/подвал по SPACING_HERO_FOOTER.

Файлы: `site/styles.css`

:root --section-pad: clamp(56px, 8vw, 96px) → clamp(<GAP_MOBILE>px, 8vw, <GAP_DESKTOP>px) (по умолчанию clamp(48px, 8vw, 80px); наклон 8vw не менять — меняются только концы ряда). Прецедент (закрывает C6): .precedent__container { padding-block: 0 clamp(48px, 7vw, 80px); } → { padding-block: 0 var(--section-pad); }; новый токен :root --precedent-reserve: <PRECEDENT_RESERVE>px (80px); .precedent-card { margin-top: clamp(120px, 13vw, 192px); } → { margin-top: var(--precedent-reserve); } с комментарием «48px выступа портрета + 32px воздуха по проектному ряду; замер SPACING-MEASUREMENTS-2026-09-06 показал 235px пустоты при 187»; правило ≤1200 .precedent-card { margin-top: clamp(40px, 6vw, 64px); } оставить (портрет скрыт). Локальный override .facts (32/20 из этапа 5) не трогать. HERO_MINH=keep: .hero min-height не менять; .hero__body паддинги в site/ и FINAL-DEV1-HERO builder-CSS не трогать. SPACING_HERO_FOOTER=measure-only (по умолчанию): .site-footer__inner 44/28 не менять; при значении align (только по ответу владельца): .site-footer__inner padding-top → var(--section-pad), padding-bottom 28 → 48 на desktop, и hero bottom-паддинг в build-hero-variants.py с bump FINAL-DEV1-HERO — не делать без ответа. Проверить, что с новым запасом портрет не наезжает на секцию услуг на 1201, 1280, 1440, 1920 (портрет clamp(360px,40vw,520px), карточка растёт от контента).

Проверка: grep -n -- '--section-pad:\|--precedent-reserve:\|precedent__container\|\.precedent-card {' -A1 site/styles.css → новые значения; после сборки: python scripts/measure-section-gaps.py http://127.0.0.1:8098/build/variants/final-dev4/ --viewports 1440x900,1440x1200,1280x900,1201x900,390x844,390x740 --expect-desktop <GAP_DESKTOP> --expect-mobile <GAP_MOBILE> --allow hero,facts,footer --baseline docs/design-references/measurements/<ДАТА>-gaps-before.json --json docs/design-references/measurements/<ДАТА>-gaps-after.json → exit 0; в таблице «Прецедент»: от точек услуг до макушки портрета на 1440 ≈ 80 + 80 − 48 = 112 (было 235), наезда нет; grep -c 'min-height: clamp(560px, 84vh, 880px)' site/styles.css → 1

### 10. Часть B (только при FONT_HEADINGS и EXTEND_UNMARKED): физические woff2 семейства заголовков тем же механизмом, что build-font-variants.py; fonts.css и preload.

Файлы: `scripts/fetch-heading-fonts.py (новый)`, `site/fonts.css`, `site/fonts/`, `site/index.html`

Скрипт (маркер HEADING-FONTS-FETCH v1.0.0 | <ДАТА>) импортирует fetch, parse_css, cyrillic_glyphs, SUBSETS/REQUIRED/CYRILLIC_RANGE из scripts/build-font-variants.py через importlib.util.spec_from_file_location (имя файла с дефисом; функции не дублировать и не рефакторить). Параметры: --family onest|golos|manrope (FONT_HEADINGS), --light (HEADING_LIGHT=300 → нижняя граница диапазона 300). Запросы css2 как в VARIANTS: onest → Onest:wght@400..800 (или 300..800 при --light), golos → Golos+Text:wght@400..900, manrope → Manrope:wght@400..800 (или 300..800); только style normal (курсив снят); поднаборы cyrillic-ext, cyrillic, latin-ext, latin; для каждого файла cmap-проверка: cyrillic ≥ 60 из 96 кодов, иначе SystemExit (история Archivo). Имена файлов по образцу site/fonts: <family-slug>-normal-<w1>-<w2>-<subset>.<8 hex sha256>.woff2. Поведение по семейству: onest без --light — файлы уже на месте (диапазон 400 800): ничего не скачивать, только проверить cmap существующих 4 файлов и напечатать отчёт; onest --light — заменить 4 файла onest-* и font-weight в @font-face на «300 800»; golos/manrope — добавить 4 новых файла и 4 блока @font-face (font-family 'Golos Text'/'Manrope', font-display swap, unicode-range из ответа API) в site/fonts.css после блоков Onest. Playfair: после шага 11 удалить блоки @font-face Playfair Display (italic — всегда; normal — только если grep 'Playfair' site/styles.css = 0, т.е. EXTEND_UNMARKED=yes) и соответствующие файлы site/fonts/playfair-display-*.woff2 (git rm); заголовочный комментарий fonts.css: «Два семейства: Onest — текст…; <семейство заголовков> — заголовки» (или «одно семейство Onest» при onest). site/index.html preload (:34–35): оставить onest cyrillic (обновить хэш при --light); playfair-display-normal-500-cyrillic → cyrillic-файл семейства заголовков (golos/manrope) либо снять вторую строку (onest — один и тот же файл уже прелоадится). Никаких обращений к Google Fonts из страницы: проверка внешних ссылок как в verify() build-font-variants.

Проверка: python scripts/fetch-heading-fonts.py --family <FONT_HEADINGS> → отчёт «кириллица N/96» ≥ 60 по каждому cyrillic-файлу; ls site/fonts | grep -c playfair → 0 при EXTEND_UNMARKED=yes (иначе 2 — только normal); for f in $(grep -o 'fonts/[^)]*woff2' site/fonts.css); do test -f site/$f || echo MISSING $f; done → пусто; grep -c 'rel="preload" href="fonts/' site/index.html → 1 (onest) или 2 (golos/manrope); grep -c 'fonts.googleapis\|fonts.gstatic' site/index.html site/fonts.css site/styles.css → 0

### 11. Часть B: заголовки без засечек и без курсива, иерархия толщиной; токен --font-serif указывает на семейство заголовков.

Файлы: `site/styles.css`

:root --font-serif: "Playfair Display", Georgia, "Times New Roman", serif → onest: "Onest", Helvetica, Arial, sans-serif; golos: "Golos Text", Helvetica, Arial, sans-serif; manrope: "Manrope", Helvetica, Arial, sans-serif (имя токена не переименовывать — build-font-variants.py подменяет `--font-serif:` по регулярке; добавить комментарий «исторически serif; с <ДАТА> семейство заголовков без засечек, решение №1»). 13 помеченных заголовков через 5 правил: .hero__title (:≈425) font-weight 500 → 700; .section-title (:≈134) 500 → 700 (обслуживает h2 услуг и h2 контактов 7.4); .svc-title (:≈789) font-style italic → normal, 500 → 600; .precedent-card__title (:≈959) italic → normal, 500 → 600; .attorney-card__name (:≈1053) italic → normal, 500 → 600. EXTEND_UNMARKED=yes: все остальные потребители var(--font-serif) по grep (.svc-media__name italic→normal 600; .form-success__title italic→normal 600; заголовки кубиков из этапа 5 — 600, если этап 5 оставил им --font-serif) — тоже без курсива; EXTEND_UNMARKED=no: их не трогать (тогда Playfair normal остаётся для них, italic-файлы всё равно уходят только если ни одно правило не italic — проверить grep 'font-style: italic'). Размеры (clamp) не менять; text-wrap balance не добавлять (C4 отдельно). Кегль H1 на 360/390: sans 700 шире Playfair 500 — если qa-browser-matrix short-mobile (360×600/668, 390×724/740) теряет CTA из первого экрана, снизить только максимум clamp .hero__title на один шаг (58 → 54px) и зафиксировать в отчёте; иных правок Hero не делать. Пересчитать 3.22 «Установление или оспаривание отцовства, тест ДНК»: на 1440 ровно 2 строки, на 390/360 без висячего слова (оговорка №20).

Проверка: grep -c 'font-style: italic' site/styles.css → 0 при EXTEND_UNMARKED=yes; grep -n 'var(--font-serif)' site/styles.css → только ожидаемые правила; после сборки: python scripts/measure-fonts.py http://127.0.0.1:8098/build/variants/final-dev4/ --expect-heading '<семейство>' --json docs/design-references/measurements/<ДАТА>-fonts-after.json → exit 0: 0 элементов с фолбэком, 0 faux, доля Playfair 0% (yes) или только непомеченные (no); в JSON у .hero__title fontWeight 700, у .svc-title 600 и fontStyle normal

### 12. Часть B: runner, дизайн-правила и locked decision под новое семейство заголовков.

Файлы: `scripts/qa-browser-matrix.py`, `docs/GAMBARIAN-DESIGN-RULES.md`, `.claude/skills/gambarian-landing-design/SKILL.md`

qa-browser-matrix.py: EXPECTED_FONTS = {target.name: ("<семейство заголовков>", "Onest") for target in PREVIEWS} (все Preview, собранные из site/: final-dev, final-dev1, final-dev3-сборка, hero-a/b, action-bar, review-numbered, final-dev4); четыре явных записи v1–v4 оставить как есть (v1 по-прежнему Playfair+Onest — он строится builder-ом шрифтов). Роль «italic» (.svc-title) переименовать в «heading» в platform_font_metrics, fontSamples и role_expectations (метки failure меняются — это и есть причина bump); добавить проверку для ролей title/heading: computed fontStyle === 'normal' → иначе failure heading-italic:<role>. RUNNER_VERSION 1.5.0 → 1.6.0, docstring :2 → PREVIEW-BROWSER-QA-RUNNER v1.6.0 | <ДАТА>, строка 13 docstring («Font coverage includes title, italic service heading» → «service heading»). GAMBARIAN-DESIGN-RULES.md: версия 1.0.0 → 2.0.0 (смена шрифтов = major по §Versioning), таблица Locked visual system строка «Шрифты» → «<семейство> для заголовков без курсива, иерархия толщиной (700/700/600); Onest для интерфейса и текста», дата. SKILL.md: «Preserve Playfair Display + Onest» → «Preserve <семейство> (заголовки) + Onest (текст)», версия GAM-DESIGN-SKILL — минорный bump + дата. docs/FONT-VARIANTS.md не трогать (история).

Проверка: python scripts/qa-browser-matrix.py http://127.0.0.1:8098/build/variants/final-dev4/ --target-name final-dev4 → все cells PASS без platform-font-fallback / expected-family / heading-italic; python scripts/qa-browser-matrix.py http://127.0.0.1:8098/ --all-previews → summary PASS (итог ячеек как после этапа 6), включая v1–v4; grep -n 'RUNNER_VERSION\|PREVIEW-BROWSER-QA-RUNNER v' scripts/qa-browser-matrix.py → 1.6.0 в обоих местах; grep -c 'Playfair' docs/GAMBARIAN-DESIGN-RULES.md .claude/skills/gambarian-landing-design/SKILL.md → 0 0 (кроме истории, если есть)

### 13. Поднять FINAL-DEV4-DESIGN и синхронизировать маркер во всех источниках.

Файлы: `scripts/final_dev4_contract.py`, `docs/boards/2026-08-06-versions-links.md`, `docs/tasks/ (документ TASK_PATH из final_dev4_contract.py)`, `site-addons/final-dev4/ (заголовок адаптера, если этап 4 включил его в проверку маркера)`

VERSION — минорный bump относительно значения после этапа 6 (изменился визуальный output dev4: шрифты/фото/отступы), DATE <ДАТА>; тот же маркер везде, где verify_final_dev4_sources и verify build-hero-variants его ищут (TASK_PATH, BOARD_PATH, адаптер). HTML/CSS-маркеры сборки подхватываются builder-ом. Если Часть B отложена — bump всё равно (фото и отступы меняют output), в описании версии указать «без Части B».

Проверка: python -B scripts/build-hero-variants.py → все варианты «Проверка пройдена»; python -B scripts/verify-client-previews.py → PASS; grep -rn 'FINAL-DEV4-DESIGN v' scripts site-addons docs build/variants/final-dev4 | grep -v 'final-dev4-stage-' → одна версия/дата

### 14. Пересобрать всё, прогнать гейты, снять замеры «после», переотснять эталоны и обновить документы.

Файлы: `site/gambarian-standalone.html`, `build/variants/final-dev4 (производный)`, `docs/SPACING-MEASUREMENTS-2026-09-06.md`, `docs/FONT-AND-PHOTO-MEASUREMENTS-2026-09-06.md`, `docs/design-references/`, `docs/FINAL-QA-CHECKLIST.md`, `docs/SCREEN-COMPOSITION.md`, `docs/tasks/2026-08-10-attorney-photos-mobile.md`, `docs/CONTENT-OWNER-EDITS.md`, `docs/RESUME.md`, `docs/boards/2026-08-06-versions-links.md`

Порядок: build-preview --standalone (шрифты и картинки инлайнятся data-URI — обязательно после шагов 6–11) → build-font-variants → build-hero-variants → build-action-bar → build-review-numbered → verify-client-copy → unittest → verify-client-previews → verify-lead-hook → verify-address-links и verify-business-hours по локальному final-dev4 (регрессия этапов 3–4) → qa-browser-matrix --target-name final-dev4 и --all-previews → measure-fonts/measure-section-gaps/measure-head-top «после» с --json в docs/design-references/measurements/<ДАТА>-*-after.json → git diff --check. Дескрипторы @font-face в site/fonts.css и в site/gambarian-standalone.html совпадают (сравнить нормализованные font-family/style/weight/unicode-range; src отличается url vs data:). Эталоны в docs/design-references/: attorney-head-top-<ДАТА>-{desktop,mobile}-{0,1}-marked.png (из measure-head-top), spacing-<ДАТА>-{desktop,mobile}.jpg (из --annotate), headings-<ДАТА>-{1440,390}.png (скриншоты секций услуг/прецедента/адвокатов/контактов — только при Части B); старые эталоны 2026-09-06 не удалять (история замера «до»). SPACING-MEASUREMENTS → v1.1.0: раздел «Результат после final-dev4 этап 7» с таблицами «до → после» из скрипта для 1440 и 390, ссылка на scripts/measure-section-gaps.py и JSON; FONT-AND-PHOTO-MEASUREMENTS → v1.1.0: таблица отрисованных шрифтов «после» (или пометка «Часть B отложена»), таблица верха головы «после» (≤3px) и ссылка на crop-attorney-cards.py; оба — с ## Related. FINAL-QA-CHECKLIST: §5 — новые пункты (единый кадр PHOTO_RATIO на всех ширинах, разница верха головы ≤3px гейтом measure-head-top, мобильная компенсация снята), §6 — семейство заголовков и гейт measure-fonts (при Части B), §2 — Build tools 1.2.0, Client Copy contract patch, runner 1.6.0 (Часть B), §16 — C2 → PASS, C6 → PASS; версия чек-листа patch + дата. SCREEN-COMPOSITION.md: экраны 6 (адвокат) и 5 (прецедент) — кадр и запас, patch-версия. docs/tasks/2026-08-10-attorney-photos-mobile.md: в шапку одну строку «superseded 2026-09-06 решением владельца №3 (final-dev4, этап 7): единый кадр на всех ширинах, компенсация object-position снята» (строку добавлять только если компенсация действительно снята) — остальное не править. CONTENT-OWNER-EDITS.md: раздел «Решения владельца 2026-09-06: шрифты (№1–2), фото (№3), отступы (№28)» с фактически применёнными параметрами и списком «отложено», версия документа — следующая минорная. RESUME.md таблица контрактов и boards «Версии контрактов»: Build tools, Client Copy contract, runner, FINAL-DEV4-DESIGN — новые значения.

Проверка: все команды раздела «Гейты» с кодом 0; ls docs/design-references/measurements/ → 6 JSON (3 before + 3 after); grep -c 'measure-section-gaps' docs/SPACING-MEASUREMENTS-2026-09-06.md → ≥1; grep -c 'crop-attorney-cards' docs/FONT-AND-PHOTO-MEASUREMENTS-2026-09-06.md docs/source-photos/README.md → ≥1 в каждом; grep -n '| C2 \|| C6 ' docs/FINAL-QA-CHECKLIST.md → PASS; git diff --check → пусто

### 15. Один коммит, push, draft PR в main с отчётом; после деплоя владельцем — live-приёмка.

Файлы: `.github/PULL_REQUEST_TEMPLATE.md`, `docs/tasks/codex/2026-09-06-final-dev4-stage-7.md`

Коммит `feat(final-dev4): sans headings, equal attorney crops, unified section spacing` (если Часть B отложена — `feat(final-dev4): equal attorney crops, unified section spacing, measurement gates`); без идентификаторов моделей и trailer-ов. git push -u origin codex/final-dev4-s7-fonts-photos-spacing. Draft PR в main по шаблону (Type: New feature + Documentation; Related: PR этапа 6) с отчётом по разделу «Отчёт». Деплой запускает владелец: Actions → Deploy Previews → Run workflow → ветка этапа → only=final-dev4. До деплоя снять SHA-256 final-dev3 и production (curl -sS -A gambarian-readback … | sha256sum; в PowerShell curl.exe + Get-FileHash). После деплоя: python -B scripts/verify-live-previews.py --only final-dev4; python scripts/measure-fonts.py https://final-dev4.gambarian-landing.pages.dev/ --expect-heading '<семейство>' (при Части B); python scripts/measure-section-gaps.py https://final-dev4.gambarian-landing.pages.dev/ --expect-desktop <GAP_DESKTOP> --expect-mobile <GAP_MOBILE> --allow hero,facts,footer; python scripts/measure-head-top.py --url https://final-dev4.gambarian-landing.pages.dev/ --out /tmp/head-live; python scripts/qa-browser-matrix.py https://final-dev4.gambarian-landing.pages.dev/ --target-name final-dev4; curl -sS -A gambarian-readback https://final-dev4.gambarian-landing.pages.dev/ | grep -c 'card-v3' → 4 и | grep -c 'playfair' → 0 (Часть B); повторить SHA-256 final-dev3 и production — совпадают (production 656CBCD0…C13E22). Допилить proof-блок в PR.

Проверка: git log -1 --format=%H; git status --short → пусто; ссылка на draft PR; CI зелёный; после деплоя — все live-команды с кодом 0 и SHA-256 до/после совпадают

## Гейты (в этом порядке)

- `python -B scripts/build-preview.py site/gambarian-standalone.html --standalone`
- `python -B scripts/build-font-variants.py`
- `python -B scripts/build-hero-variants.py`
- `python -B scripts/build-action-bar.py`
- `python -B scripts/build-review-numbered.py`
- `python -B scripts/verify-client-copy.py`
- `python -m unittest discover -s scripts/tests`
- `python -B scripts/verify-client-previews.py`
- `node scripts/verify-lead-hook.mjs`
- `python -m http.server 8098 (отдельный терминал, из корня) && python scripts/verify-address-links.py http://127.0.0.1:8098/build/variants/final-dev4/`
- `python scripts/verify-business-hours.py http://127.0.0.1:8098/build/variants/final-dev4/`
- `python scripts/measure-fonts.py http://127.0.0.1:8098/build/variants/final-dev4/ --expect-heading '<FONT_HEADINGS-семейство>' --json docs/design-references/measurements/<ДАТА>-fonts-after.json (без --expect-heading, если Часть B отложена; 0 фолбэков на 1440 и 390 обязательно в любом случае)`
- `python scripts/measure-head-top.py --url http://127.0.0.1:8098/build/variants/final-dev4/ --out docs/design-references/ --json docs/design-references/measurements/<ДАТА>-head-after.json (разница ≤3px на 1440 и 390)`
- `python scripts/measure-section-gaps.py http://127.0.0.1:8098/build/variants/final-dev4/ --viewports 1440x900,1440x1200,1280x900,1201x900,390x844,390x740 --expect-desktop <GAP_DESKTOP> --expect-mobile <GAP_MOBILE> --allow hero,facts,footer --baseline docs/design-references/measurements/<ДАТА>-gaps-before.json --json docs/design-references/measurements/<ДАТА>-gaps-after.json`
- `python scripts/qa-browser-matrix.py http://127.0.0.1:8098/build/variants/final-dev4/ --target-name final-dev4`
- `python scripts/qa-browser-matrix.py http://127.0.0.1:8098/ --all-previews`
- `git diff --check`
- `после деплоя владельцем (Deploy Previews → ветка codex/final-dev4-s7-fonts-photos-spacing → only=final-dev4): python -B scripts/verify-live-previews.py --only final-dev4`
- `после деплоя: measure-fonts / measure-section-gaps / measure-head-top / qa-browser-matrix --target-name final-dev4 по https://final-dev4.gambarian-landing.pages.dev/ (коды 0)`
- `после деплоя: curl -sS -A gambarian-readback https://final-dev3.gambarian-landing.pages.dev/ | sha256sum и curl -sS -A gambarian-readback https://gambarian-landing.pages.dev/ | sha256sum — совпадают с замером до деплоя (production 656CBCD0…C13E22)`

## Версии и маркеры

- requirements-build.txt: BUILD-TOOLS 1.1.1 → 1.2.0 | <ДАТА> (+ Pillow) → docs/FINAL-QA-CHECKLIST.md §2
- scripts/client_copy_contract.py: CLIENT-COPY-CONTRACT — 1.4.0 → 1.4.1 (после этапа 5) + <ДАТА> (токены src/srcset yulia-card-v3): docstring :3, CONTRACT_VERSION/DATE и все документы по grep 'CLIENT-COPY-CONTRACT v\|Client Copy contract' (RESUME, FINAL-QA-CHECKLIST, boards, CONTENT-SOURCE-MAP, CONTENT-EXTRA, dark-fact-cards.md, CLIENT-PREVIEW-HANDOFF)
- scripts/final_dev4_contract.py: FINAL-DEV4-DESIGN — минорный bump относительно значения после этапа 6 + <ДАТА> → TASK_PATH, BOARD_PATH, адаптер site-addons/final-dev4, HTML/CSS сборки
- scripts/qa-browser-matrix.py (только Часть B): PREVIEW-BROWSER-QA-RUNNER 1.5.0 → 1.6.0 | <ДАТА> (docstring :2, RUNNER_VERSION; EXPECTED_FONTS, роль heading) → RESUME, boards, FINAL-QA-CHECKLIST
- docs/GAMBARIAN-DESIGN-RULES.md (только Часть B): 1.0.0 → 2.0.0 | <ДАТА> (смена шрифтов = major); .claude/skills/gambarian-landing-design/SKILL.md GAM-DESIGN-SKILL — минорный bump + дата
- Новые скрипты: FONT-MEASURE v1.0.0, SECTION-GAPS-MEASURE v1.0.0, HEAD-TOP-MEASURE v1.0.0, ATTORNEY-CARD-CROP v1.0.0, HEADING-FONTS-FETCH v1.0.0 (Часть B) — все | <ДАТА>
- Документы: SPACING-MEASUREMENTS 1.0.0 → 1.1.0; FONT-AND-PHOTO-MEASUREMENTS 1.0.0 → 1.1.0; FINAL-QA-CHECKLIST patch; SCREEN-COMPOSITION 2.2.0 → 2.2.1; CONTENT-OWNER-EDITS — следующая минорная; все с датой <ДАТА>
- Не меняются: LIVE-PREVIEW-READBACK и NBSP_EXPECTED (текст не менялся), ACTION-BAR-SPEC, FINAL-DEV1-HERO (при SPACING_HERO_FOOTER=measure-only), FINAL-DEV3-DESIGN 2.0.2, карта Preview 2.5.0, Lead schema 2.0.0, CLIENT-COPY-VERIFIER, REVIEW-NUMBERED, font-variants v2/v3 mobile markers

## Приёмка этапа

- [ ] Три скрипта замера закоммичены с маркерами FONT-MEASURE v1.0.0, SECTION-GAPS-MEASURE v1.0.0, HEAD-TOP-MEASURE v1.0.0; на сборке «до» они воспроизводят цифры документов 2026-09-06 (0 фолбэков, Onest ≈90%; 96/96 у секций; верх головы 75/30 и 38/20), гейты падают там, где должны (--expect-heading, --expect-*, --max-diff)
- [ ] Фото: site/assets содержит 12 файлов *-card-v3-*, старые card-v2/yulia-card удалены; manifest.json обновлён; обе img 1100×825 (4:3), 1100×1100 (1:1) или 1100×1375 (пустой PHOTO_RATIO); measure-head-top на 1440 и 390 — разница ≤3px; лица целиком, у Юлии кадр не масштабирован вверх и не дорисован; на 1440 высота фото ≈418px (4:3) при ширине 558
- [ ] CSS: одна .attorney-photo aspect-ratio на всех ширинах; правил 16 / 15 и object-position center 16% нет; orphan-класс attorney-photo--yulia снят (C2 PASS)
- [ ] Отступы: --section-pad clamp(<GAP_MOBILE>px, 8vw, <GAP_DESKTOP>px); .precedent__container и .precedent-card на токенах (C6 PASS); measure-section-gaps с --expect 80/48 → exit 0, наезда портрета нет на 1201/1280/1440/1920; кремовое поле фактов 32/20 сохранено; min-height Hero без изменений; Hero/подвал замерены и вынесены вопросом
- [ ] Часть B (если выполнена): measure-fonts --expect-heading → 0 фолбэков, 0 faux, 0 italic на 1440 и 390; grep 'font-style: italic' site/styles.css = 0 (EXTEND_UNMARKED=yes); каждая объявленная грань физически есть в site/fonts/ и cmap ≥60/96; preload ведёт на существующие файлы; дескрипторы @font-face в fonts.css и standalone совпадают; нет внешних ссылок на шрифты; runner 1.6.0 с EXPECTED_FONTS под новое семейство, --all-previews PASS (включая v1–v4); GAMBARIAN-DESIGN-RULES 2.0.0 и SKILL.md обновлены
- [ ] Часть B (если отложена): в отчёте явно «отложено: ждёт ответа №1/№2», Playfair и EXPECTED_FONTS не тронуты, measure-fonts без --expect-heading → 0 фолбэков
- [ ] Контракты: CLIENT-COPY-CONTRACT patch bump с новыми src/srcset токенами Юлии — одна версия/дата во всех источниках; unit-тест структуры Юлии на новых именах; BUILD-TOOLS 1.2.0 (Pillow); FINAL-DEV4-DESIGN минорный bump синхронизирован; NBSP_EXPECTED и число «&nbsp;—» не изменились
- [ ] Все гейты раздела «Гейты» код 0; qa-browser-matrix без overflow на 360/390/960/961/1440, Hero CTA в первом экране на 360×600/360×668/390×724/390×740
- [ ] Документы: SPACING-MEASUREMENTS v1.1.0 и FONT-AND-PHOTO-MEASUREMENTS v1.1.0 с таблицами «до → после» и ссылками на скрипты/JSON; docs/source-photos/README пайплайн карточек; FINAL-QA-CHECKLIST §2/§5/§6/§16; CONTENT-OWNER-EDITS решения №1–3/28; attorney-photos-mobile.md помечен superseded; эталоны <ДАТА> в design-references
- [ ] После деплоя only=final-dev4: verify-live-previews PASS; live measure-* PASS; curl final-dev4 содержит card-v3 и (Часть B) не содержит playfair; SHA-256 final-dev3 и production до/после совпадают

## Отчёт в PR (обязательные поля)

- Первой строкой — фактические значения параметров этапа: FONT_HEADINGS, EXTEND_UNMARKED, HEADING_LIGHT, PHOTO_RATIO, GAP_DESKTOP, GAP_MOBILE, PRECEDENT_RESERVE, HERO_MINH, SPACING_HERO_FOOTER и статус Части B (выполнена / отложена: ждёт ответа №1/№2)
- Хэш коммита, подтверждение push в origin/codex/final-dev4-s7-fonts-photos-spacing, ссылка на draft PR и на PR этапа 6 (база)
- Diff-доказательство: git diff --stat; ключевые фрагменты — токены :root (--font-serif, --section-pad, --precedent-reserve), 5 правил заголовков, .attorney-photo и удалённые mobile-правила, .precedent__container/.precedent-card, picture/srcset карточек, OWNER_APPROVED_HTML_TOKENS, EXPECTED_FONTS/RUNNER_VERSION, fonts.css @font-face, preload; явно: git diff docs/sources пустой, тексты не менялись, число «&nbsp;—» прежнее
- Таблицы замеров «до → после» (вставить из скриптов): шрифты (семейство / глифов / доля / элементов с фолбэком на 1440 и 390), отступы по секциям (1440 и 390, включая Hero и подвал), верх головы (px и %, разница) — с путями к JSON в docs/design-references/measurements/
- Дословный вывод гейтов: verify-client-copy (PASS-строка с новой версией контракта), unittest, verify-client-previews, build-review-numbered, verify-lead-hook, verify-address-links, verify-business-hours, measure-fonts, measure-head-top, measure-section-gaps, qa-browser-matrix summary (target и --all-previews), git diff --check
- Скриншоты 1440/390: карточки адвокатов (обе), прецедент с портретом, стык услуги → прецедент, секция услуг и Hero (при Части B — заголовки без засечек) — как визуальная проверка
- Таблица маркеров «было → стало» по всем файлам раздела «Версии»
- Проверено / Не проверено: до деплоя — только локальная сборка; не проверено — реальный iPhone (safe-area, рендер шрифта), мониторы выше 1200px по высоте (Hero min-height), 1920 (кадр Hero); после деплоя — live measure-*, verify-live-previews, SHA-256 final-dev3/production
- Статус строк списка владельцев: 2, 15, 18, 20, 23, 26, 28, 31, 36, 45, 51, 59, 69 (пометка «Поменять шрифт») — сделано / отложено; 94 (шрифты, фото, отступы — три абзаца) — по каждому абзацу; 40 (фото Юли в «Ведёт») — отложено (№18); со ссылкой на коммит
- Вопросы владельцу: (1) если Часть B отложена — выбор FONT_HEADINGS и EXTEND_UNMARKED; (2) HEADING_LIGHT — какие подзаголовки набрать тонким (300), если вообще; (3) SPACING_HERO_FOOTER — подтянуть ли подвал 44/28 и низ Hero к ряду 80/48 (замеры приложены) или оставить как есть; (4) пропорция фото — если PHOTO_RATIO был пуст, выбрать 4:3 или 1:1; если применялся 4:3, подтвердить кадр на телефоне (фото ниже, чем по решению 2026-08-10: ≈236px вместо 294 при ширине 314); (5) уменьшить ли .hero__title на 360px, если понадобилось для CTA

## Риски

- Догадка вместо ответа владельца по семейству/расширению на непомеченные заголовки нарушает spec «Не делать без ответа»: при пустых FONT_HEADINGS/EXTEND_UNMARKED Часть B пропускается целиком, а не выполняется «по рекомендации»
- Переименование токена --font-serif ломает build-font-variants.py (re.sub по имени токена и проверка семейства в styles) — имя оставить, менять только значение
- Ассеты v3 с новыми хэшами: забытая правка любого из мест (OWNER_APPROVED_HTML_TOKENS, тест-фикстура, deploy-pages.sh/ps1, manifest.json, standalone) валит verify-client-copy или production-readback; список мест в шаге 7 полный — сверить grep -rn 'card-v2\|yulia-card-' по репо после правок (кроме docs-истории)
- У Юлии верх головы на 1.3% исходника: любой кадр с масштабированием вверх или дорисовкой запрещён; при 4:3 воздуха над волосами ≈2% — если владелец сочтёт тесно, единственный честный выход — новое фото, не object-position
- Одинаковый масштаб лица зависит от ручной точки chin: зафиксировать её в README с размеченным кадром; детектор верха головы на сером фоне Юлии чувствителен к порогу — проверять отметки глазами, как требует метод
- Кадр 4:3 на телефоне ниже, чем решение владельца 2026-08-10 (16/15): это замена решения — явно показать в отчёте и вопросах (оговорка №3 п.2); при пустом PHOTO_RATIO замены решения нет
- Sans 700 в H1 шире Playfair 500: на 360×600/668 CTA может выйти из первого экрана (гейт runner short-mobile) — допустим только один шаг кегля H1, любые другие правки Hero запрещены (FINAL-DEV1-HERO)
- Снятие italic-файлов Playfair при EXTEND_UNMARKED=no невозможно, пока хоть одно правило italic — проверять grep, а не предполагать
- Портрет прецедента при запасе 80 может наезжать на секцию услуг на ширинах 1201–1300 (портрет 40vw, карточка растёт от контента) — гейт measure-section-gaps на 1201/1280 обязателен
- Изменение --section-pad меняет все Preview из site/ и font-variants; их «межблочные отступы не меняются» в RESUME/boards устаревает — отметить в документах, деплой только only=final-dev4
- Замер «до» должен быть снят ДО правок и закоммичен (JSON) — иначе колонка «до» в таблицах недоказуема
- Pillow не закреплён в requirements-build.txt, хотя grade-hero-photo.py его импортирует; CI (deploy-previews.yml) ставит только requirements — без bump BUILD-TOOLS crop-скрипт в CI не запустится (он и не нужен в CI, но зависимость фиксировать)
- В облачном контейнере Chromium запускается только с executable_path из PLAYWRIGHT_BROWSERS_PATH и без сети — скрипты обязаны поддерживать локальный http и переменную окружения; live-замеры делать с машины с сетью
- Workflow с пустым only опубликует все alias, включая final-dev3 — поле only=final-dev4 обязательно

## Проверка карточки критиком

скоуп: ЗАМЕЧАНИЯ; пути: ок; гейты: ок; промпт: ок.

Лишнее / не в своём этапе: `Шаги 6–8 и PHOTO_RATIO=4:3: перекадрирование под новый кадр 4:3 и смена .attorney-photo aspect-ratio 4/5 → 4/3 реализуют открытый №3 «размер и пропорция фото», у которого в таблице «Открытые вопросы» spec нет пометки «по умолчанию», а «Правила для исполнителя» п.3 прямо запрещают «размер фото» без ответа; ссылка карточки на «Порядок реализации» п.7 «(4:3 по умолчанию)» — внутреннее противоречие spec, не решение владельца`

Правки критика, обязательные к применению исполнителем:

- **Применено** (карточка переписана: «Цель», «Основание», шаги 6 и 8, приёмка и промпт работают от параметра PHOTO_RATIO, пустое значение = пропорцию не менять). Исходное замечание: сделать фото параметрическими как Часть B: при пустом PHOTO_RATIO — реализовать только решённую часть №3 (одинаковый кадр, верх головы ≤3px, перекадрирование Александра под текущее окно 4/5, Юлию не трогать/не растягивать; мобильные компенсации 16/15 и center 16% снять только если замер это подтверждает), а смену пропорции 4:3/1:1 и CSS aspect-ratio выполнять лишь при заполненном PHOTO_RATIO из реестра решений; либо архитектор фиксирует «4:3 по умолчанию» в таблице «Открытые вопросы» spec новой версией до запуска этапа
- **Применено** (шаг 1: «CONTRACT_VERSION 1.4.x (после этапа 5)»; раздел версий: «1.4.0 → 1.4.1»). Исходное замечание: заменить предпосылку «CONTRACT_VERSION 1.3.x» на «1.4.x (после этапа 5)» и «patch после этапа 6» → «1.4.0 → 1.4.1»
- **Применено с поправкой:** в карточке стоит docstring :2 — сверено по scripts/qa-browser-matrix.py:2, где и лежит маркер PREVIEW-BROWSER-QA-RUNNER (значения 30 и 13 оба неверны). Исходное замечание: исправить номер строки docstring runner (13, не 30)
- **Применено** (в блоке «Параметры этапа» промпта PHOTO_RATIO — плейсхолдер `<4:3|1:1|пусто>` с пояснением, что пустое значение оставляет окно 4/5). Исходное замечание: промпт — заполнять FONT_HEADINGS/EXTEND_UNMARKED только из реестра; сейчас плейсхолдеры <onest|golos|manrope|пусто> — допустимо, но PHOTO_RATIO=4:3 в промпте зашит жёстко и должен стать пустым/условным по той же логике

Проверено: .attorney-photo :1031, aspect-ratio 16/15 :1508, center 16% :1513, --section-pad clamp(56px,8vw,96px) :40, .precedent__container :900, .precedent-card margin-top clamp(120px,13vw,192px) :903, ≤1200 clamp(40px,6vw,64px) :1425, .hero__title :425, .section-title :134, .svc-title :789 (italic), .precedent-card__title :959, .attorney-card__name :1053, preload :34–35, sizes атрибут, attorney-photo--yulia, deploy-pages.sh:63/ps1:101 'alexander-card-v2', BUILD-TOOLS 1.1.1, fetch/parse_css/cyrillic_glyphs/SUBSETS/REQUIRED/CYRILLIC_RANGE в build-font-variants.py, роль 'italic' :234/:991, DESIGN-RULES 1.0.0, SKILL.md «Preserve Playfair Display + Onest», manifest alexander-card 2100×2200, токен yulia-card-760w.df9bd223.jpg, test_owner_approved_yulia_structure_fails, SCREEN-COMPOSITION 2.2.0, SPACING/FONT-AND-PHOTO 1.0.0, .site-footer__inner 44/28 :1340, min-height Hero :326, docs/source-photos/{alexander,yulia}-portrait.jpg. Часть B корректно заблокирована на №1/№2; отступы 80/48 и запас 80 разрешены CODEX-WORKING-MODEL. Скоуп нарушает только пропорция фото.

## Промпт для Codex

Вставить в Codex CLI в корне репозитория на ветке этапа:

```text
Ты исполнитель этапа 7 «Шрифты, фото, отступы» версии final-dev4 лендинга «Гамбарян и партнёры». Работай в корне репозитория, уровень рассуждений high.
Параметры этапа (вписаны архитектором из реестра решений; пустое = не решено): FONT_HEADINGS=<onest|golos|manrope|пусто>; EXTEND_UNMARKED=<yes|no|пусто>; HEADING_LIGHT=none; PHOTO_RATIO=<4:3|1:1|пусто — при пустом значении пропорцию 4/5 не менять, только выровнять кадры>; GAP_DESKTOP=80; GAP_MOBILE=48; PRECEDENT_RESERVE=80; HERO_MINH=keep; SPACING_HERO_FOOTER=measure-only.
Если FONT_HEADINGS или EXTEND_UNMARKED пусты — Часть B (шаги 10–12 карточки) не выполнять вовсе, в отчёте «отложено: ждёт ответа №1/№2». Догадки запрещены.
Сначала прочитай по порядку: AGENTS.md; docs/RESUME.md; docs/CODEX-WORKING-MODEL.md; docs/tasks/codex/2026-09-06-final-dev4-stage-7.md целиком; в docs/tasks/2026-09-06-final-dev4-spec.md — «Реестр решений владельца» (№1–2, 3, 28, 9), «Открытые вопросы (9) и значения по умолчанию», «Подготовительные шаги» п.6–7, «Правила для исполнителя»; в docs/tasks/2026-09-06-final-dev4-items.md — E:G-01, E:G-02, B:G-05, C:G-04, D:D-01, E:G-07, B:G-13, E:G-04, E:G-05; docs/DESIGN-RECOMMENDATIONS-2026-09-06.md №1, №2, №3, №28; docs/SPACING-MEASUREMENTS-2026-09-06.md; docs/FONT-AND-PHOTO-MEASUREMENTS-2026-09-06.md; docs/source-photos/README.md; scripts/build-font-variants.py (fetch/parse_css/cyrillic_glyphs — единственный механизм получения woff2 и проверки кириллицы по cmap).
Только потом код: site/styles.css, site/fonts.css, site/index.html (карточки адвокатов, preload), scripts/qa-browser-matrix.py, scripts/client_copy_contract.py.
Предусловие: влиты этапы 1–6 (RUNNER_VERSION 1.5.0, кубики перестроены, alias final-dev4). Ветка codex/final-dev4-s7-fonts-photos-spacing от main. Draft PR в main по .github/PULL_REQUEST_TEMPLATE.md.
Часть A (делать всегда):
1. Новые scripts/measure-fonts.py (CDP CSS.getPlatformFontsForNode по всем текстовым элементам, 1440/390, exit 1 при фолбэке/faux/чужом семействе заголовков), scripts/measure-section-gaps.py (пустоты сверху/снизу/между секциями по методу SPACING-MEASUREMENTS; --expect/--allow/--baseline/--annotate), scripts/measure-head-top.py (верх головы по методу FONT-AND-PHOTO-MEASUREMENTS; --url и --files; --max-diff 3).
2. Замер «до» на сборке после этапа 6 → docs/design-references/measurements/<ДАТА>-{fonts,gaps,head}-before.json (снять ДО правок, закоммитить).
3. scripts/crop-attorney-cards.py: кадр PHOTO_RATIO из docs/source-photos/*.jpg; опорный кадр — Юлия (полная ширина, без масштабирования вверх и дорисовки), Александр подгоняется по верху головы и масштабу лица; ассеты alexander-card-v3-*/yulia-card-v3-* 480/760/1100 jpg+webp с 8-hex хэшем; manifest.json; README пайплайн; Pillow в requirements-build.txt (BUILD-TOOLS 1.2.0).
4. site/index.html: srcset/src/width/height карточек на v3, снять класс attorney-photo--yulia; токены src/srcset в OWNER_APPROVED_HTML_TOKENS + patch bump контракта с синхронизацией всех маркеров; фикстура теста; alexander-card-v2 → v3 в deploy-pages.sh/ps1; старые ассеты git rm.
5. site/styles.css: .attorney-photo aspect-ratio 4 / 3 на всех ширинах, удалить mobile-правила 16 / 15 и center 16%; --section-pad: clamp(48px, 8vw, 80px); .precedent__container padding-block 0 var(--section-pad); --precedent-reserve: 80px и .precedent-card margin-top var(--precedent-reserve) (правило ≤1200 оставить). .facts 32/20, Hero (min-height, паддинги) и подвал не трогать — только замерить.
Часть B (только при заполненных параметрах):
6. scripts/fetch-heading-fonts.py импортирует функции build-font-variants.py через importlib и кладёт woff2 семейства заголовков в site/fonts/ с проверкой cmap (onest без light — файлы уже есть, только проверить); fonts.css/preload/standalone синхронно; Playfair-блоки и файлы убрать (italic всегда; normal — при EXTEND_UNMARKED=yes).
7. --font-serif (имя токена НЕ менять) → семейство заголовков; .hero__title 700, .section-title 700, .svc-title/.precedent-card__title/.attorney-card__name 600, font-style normal; при EXTEND_UNMARKED=yes — все остальные потребители var(--font-serif) по grep.
8. scripts/qa-browser-matrix.py: EXPECTED_FONTS (<семейство>, Onest) для всех не-v alias, роль italic → heading + проверка fontStyle normal, RUNNER 1.6.0; docs/GAMBARIAN-DESIGN-RULES.md 2.0.0; SKILL.md locked decision.
Затем: FINAL-DEV4-DESIGN минорный bump синхронно (contract.py, адаптер, TASK_PATH, BOARD_PATH).
Пересобрать: build-preview --standalone (обязательно), build-font-variants, build-hero-variants, build-action-bar, build-review-numbered.
Гейты: verify-client-copy; python -m unittest discover -s scripts/tests; verify-client-previews; node scripts/verify-lead-hook.mjs; python -m http.server 8098 из корня и по http://127.0.0.1:8098/build/variants/final-dev4/: verify-address-links, verify-business-hours, measure-fonts (--expect-heading при Части B), measure-head-top (≤3px), measure-section-gaps --viewports 1440x900,1440x1200,1280x900,1201x900,390x844,390x740 --expect-desktop 80 --expect-mobile 48 --allow hero,facts,footer --baseline <before.json>; qa-browser-matrix --target-name final-dev4 и http://127.0.0.1:8098/ --all-previews; git diff --check.
Замеры «после» → docs/design-references/measurements/<ДАТА>-*-after.json; эталоны attorney-head-top-<ДАТА>-*, spacing-<ДАТА>-*, headings-<ДАТА>-* в docs/design-references/.
Документы: таблицы «до → после» в SPACING-MEASUREMENTS (v1.1.0) и FONT-AND-PHOTO-MEASUREMENTS (v1.1.0); FINAL-QA-CHECKLIST §2/§5/§6, C2/C6 → PASS; SCREEN-COMPOSITION patch; CONTENT-OWNER-EDITS (решения №1–3/28 с фактическими параметрами и списком отложенного); docs/tasks/2026-08-10-attorney-photos-mobile.md — одна строка superseded; RESUME/boards — версии.
Не делай: не выбирай семейство/расширение сам; не дорисовывай и не масштабируй вверх фото Юлии; не собирай аватар Юлии для «Ведёт» (№18); не трогай тексты, JSON-LD, alt, Action Bar, site-addons/final-dev3, FINAL-DEV1-HERO, min-height Hero, подвал, кремовое поле фактов, font-variants v1–v4, final-dev3, production, build/ руками; не запускай wrangler; поле only пустым не оставлять; идентификаторы моделей в коммит/PR не писать.
Если решения не хватает — остановись на пункте, сделай остальное, опиши вопрос в отчёте.
Один коммит `feat(final-dev4): sans headings, equal attorney crops, unified section spacing` (без Части B — `feat(final-dev4): equal attorney crops, unified section spacing, measurement gates`), push, draft PR.
Отчёт в PR: первой строкой параметры и статус Части B; хэш и push; diff-доказательство; таблицы «до → после» шрифтов/отступов/верха головы; дословный вывод всех гейтов; скриншоты 1440/390; таблица маркеров «было → стало»; «Проверено / Не проверено / Вопросы владельцу» (семейство/расширение, тонкие подзаголовки, подвал и Hero в ряду 80/48, кадр 4:3 на телефоне, кегль H1 на 360).
Деплой делает владелец (Actions → Deploy Previews → ветка этапа → only=final-dev4). До деплоя сними SHA-256 final-dev3 и production (curl -sS -A gambarian-readback … | sha256sum; в PowerShell curl.exe + Get-FileHash).
После деплоя: python -B scripts/verify-live-previews.py --only final-dev4; measure-fonts/measure-section-gaps/measure-head-top и qa-browser-matrix --target-name final-dev4 по https://final-dev4.gambarian-landing.pages.dev/; curl final-dev4 | grep -c card-v3 → 4; повтори SHA-256 — должны совпасть (production 656CBCD0…C13E22); допиши proof-блок в PR.
```

## Related

- [Модель работы с Codex](../../CODEX-WORKING-MODEL.md)
- [Задание final-dev4](../2026-09-06-final-dev4-spec.md)
- [Разбор по пунктам](../2026-09-06-final-dev4-items.md)
- [Реестр текстов владельца](../../CONTENT-OWNER-REVISIONS-2026-09-06.md)
- [Рекомендации дизайнеров](../../DESIGN-RECOMMENDATIONS-2026-09-06.md)
